# Test config: Dry run mode
# Tests: shows what would be done without executing

api_keys:
  gemini: null

llm:
  model: "gemini-2.5-flash-lite"
  mode: "api"

defaults:
  project: null
  version: null

metrics:
  mode: "basic"
  bundle: null

annotation:
  skip: false  # Would run annotation

calibration:
  skip: false  # Would run calibration

simulation:
  enable: true  # Would run simulation

pipeline:
  auto_yes: true
  verbose: true

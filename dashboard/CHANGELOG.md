# Changelog

All notable changes to `evalyn-dashboard` are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Fixed

- **`handleTest`: upgraded to synchronous in-flight ref (true same-frame protection).** Last tick discovered that the wrapper-level guards added in earlier ticks (`if (testing) return`, `if (vacuum.state === 'pending') return`) only catch CROSS-frame double-clicks, not actual same-frame ones — both closures see the React state's stale value because `setTesting(true)` is queued. handleTest is the highest-cost target of the bug class because each `settingsApi.test()` call hits the LLM provider for a 1-token completion - same-frame double-fire literally doubles the user's API spend per click. Upgraded to the same `useRef` + synchronous-mutate pattern as the copilot send fix from last tick. The other handlers (handleVacuum, handlePrune, handleSave, etc.) still use the React-state guard; their cost on same-frame double-fire is mostly wasted work (server-serialized) rather than real money, so the upgrade priority is lower. Documented as a known limitation in the relevant comments.
- **Co-pilot `send()`: same-frame double-send no longer creates two threads.** The CoPilotDock + CoPilotThread submit wrappers gate on `status === 'streaming' || pending`, but those guards read React state via closure and the disabled-attribute commits on next render - so two same-frame Cmd+Enters at the start of a fresh thread would BOTH enter `submit()`, BOTH pass the wrapper guard (closure status='idle'), and BOTH call `useCoPilotThread.send()`. Inside `send` the `setStatus('streaming')` is also queued (not committed sync), so two parallel `api.startAgentThread()` calls fire. Backend creates two threads, the FE sets `threadId` to whichever response lands last, and the user's other conversation is orphaned (audit log shows the extra thread, but the FE has no UI for it). Fixed via a synchronously-mutated `sendInFlightRef` inside the hook itself: the second call sees the ref=true and early-outs before queueing any state. Defense-in-depth - protects programmatic / future callers regardless of wrapper. New `F6` vitest case pins the behavior; verified by reverting the guard and watching `expect(startAgentThreadMock).toHaveBeenCalledTimes(1)` fail with "called 2 times".
- **Test connection + Run-all health check: same-frame double-click guards.** Two more handlers caught by the audit-grep series. Per-provider `handleTest` (Settings ProviderCard's "Test connection" button) and `onRunAll` (BulkTestCard's "Run health check") both relied solely on `disabled={testing}` / `disabled={running !== null}` to gate. Without an explicit `if (testing) return` (or running-equivalent), two same-frame clicks would both fire `settingsApi.test()` - which hits the LLM provider for a 1-token completion. Doubled token spend, possible rate-limit trip, redundant audit-log entries on the backend. Both handlers now early-out on the in-flight state.
- **Vacuum + Prune Settings buttons: same-frame double-click guards.** Continuing last tick's audit-grep of async handlers without explicit pending-state early-outs. Both `handleVacuum` and `handlePrune` in Settings's SystemStatusCard relied solely on the button's `disabled={state === 'pending'}` attribute to gate. Without an explicit `if (vacuum.state === 'pending') return` (or prune equivalent) inside the handler, two onClick events landing in the same React frame would BOTH start a vacuum/prune POST: wasted work, doubled audit-log entries, possible 503 if the server's per-route lock fires. handlePrune is already wrapped in `useArmedConfirm` (two-click sequence) but the same race exists in the second-click branch — useArmedConfirm's `setArmed(false)` is async, so two same-frame second-clicks both see armed=true (stale closure) and both proceed past the guard. Both handlers now early-out on the in-flight pending state. Other audited handlers (`handleSave`, `handleRerun`, `handleStartSession`, `handleChange` after last tick) already had the guard.
- **ActiveProviderCard: same-frame double-trigger guard.** `handleSave` (in the per-provider card) had an explicit `if (saving || !dirty) return` guard with a comment explaining the React-batched-update race window. The parallel `handleChange` (in `ActiveProviderCard`) only checked `next === data.active` and relied solely on `disabled={pending}` to gate the select. Two onChange events firing before the disabled attribute lands would both sail through — the second sees `pending=false` (stale closure). For real user clicks the window is sub-frame, but a programmatic `dispatchEvent` or a flaky synthetic test event would race past. Added the same `if (pending) return` early-out to match the `handleSave` pattern. No customer-visible change in normal use; cleaner contract.
- **`Retry-After: 0` no longer collapses to a synthetic 5s wait.** Continuing the audit-grep series that fixed Datasets sort and ExperimentsList lineage. The capacity 503 parser at `errors.ts:62` used `parseInt(retryHeader, 10) || 5`. `parseInt('0', 10)` is 0, and `0 || 5` collapses to 5 in JS - so a load balancer returning `Retry-After: 0` ("retry immediately") would force a synthetic 5-second wait. Customer impact: low frequency in current servers, but a real cliff when LBs do return 0. Now uses `Number.isFinite(parsed) && parsed >= 0` so 0 stays as 0; NaN, missing, and negative all fall back to 5. New `maybeParseCapacityError` test block (6 cases): non-503 returns null, well-formed 503, Retry-After: 0 preserved, missing header → 5, unparseable → 5, negative → 5. Verified the regression test catches the bug by reverting to `|| 5` and watching 2 cases fail.
- **ExperimentsList lineage sort: epoch-zero timestamps no longer misclassified as malformed.** The synthesized lineage sort used `Date.parse(iso) || -Infinity`. `Date.parse('1970-01-01T00:00:00Z')` returns 0 (legitimate epoch), and `0 || -Infinity` collapses to -Infinity - so a real epoch-zero timestamp would sink to the bottom alongside truly malformed rows. Customer impact: vanishingly low for eval runs (none from 1970), but the same foot-gun caught last tick in Datasets recent-sort. Audit-grep for `Date.parse.*||` found this remaining instance; both now use the `Number.isFinite(t) ? t : sentinel` shape via a small `tsOrSentinel` helper.
- **Datasets "Recent" sort: unparseable timestamps now sink last instead of sorting as 1970-epoch.** The recent-sort path used `Date.parse(iso) || 0` for non-null timestamps - any string `Date.parse` couldn't read returned NaN, and `NaN || 0` collapses to 0, which sorts the row ABOVE the null-last sentinel of -1 (treats malformed as "older than any real run" but still real). Customer impact: a row with a corrupted timestamp would surface in the middle of the list instead of sinking to the bottom where "no date" rows live - confusing because the row's listed timestamp was unreadable. Now wraps the parse in a `recentTs` helper that returns -1 for both `null` AND unparseable inputs, unifying the two "no usable date" cases. The existing -1 sentinel + ascending null-handling now treats them identically.

### Changed

- **Standardized backend error response shape on `{"detail": ...}`.** Two of the dashboard's error response sites (CSRFMiddleware's 403 + spa_fallback's API 404) returned `{"error": "..."}` while every HTTPException across the api/* modules used `{"detail": "..."}` (FastAPI's default). FE error parsers mostly `res.text()` and surface the body verbatim - keying off two JSON shapes made debug logs noisier and forced one shim (`settings.ts:95: detail = j.error ?? j.detail ?? ${res.status}`) to handle both. Now both backend sites use `detail` to match the rest. The settings.ts shim still works because the `?? j.detail` fallback hits the new key. 2 new pytest cases pin both shapes (`test_unknown_api_route_returns_detail_shape`, `test_csrf_rejection_uses_detail_shape`); verified the test catches the regression by reverting `detail` -> `error` and watching it fail. 456 backend tests pass (was 454 + 2 new).
- **Pinned vacuum + prune 503-when-persistence-unavailable behavior.** Tick from earlier this week flipped vacuum's `persistence_unavailable` from 404 (permanent: "endpoint never existed") to 503 (transient: "service unavailable, retry shortly") to align with prune's existing behavior - so a single FE error branch covers both admin endpoints. The fix shipped without a regression test; this back-fills it. Two new pytest cases monkey-patch `_persistence_for` to return None and assert each endpoint returns 503 with `detail=persistence_unavailable`. Verified the test catches the regression by temporarily flipping the code back to 404 and watching the test fail. 456 backend tests pass.
- **`useFlashState` gains imperative `reset()`; Settings.tsx `success` + `saveSuccess` now migrated.** Last tick documented two callsites that couldn't migrate to `useFlashState` because they had a load-bearing pre-API reset (`setX(false)` BEFORE the await) that the hook didn't directly support. Added an imperative `reset()` method that drops state to initial without scheduling any timer (cancels in-flight timer too). Both `ActiveProviderCard` (active-provider switch flash) and `ProviderCard.handleSave` (API-key save flash) now use `useFlashState(false)` + `resetX()` + `flashX(true, 2000)`. The pre-API clear preserves existing UX (stale "Saved" pill drops before the new save resolves) and gains unmount cleanup for free. 2 new test cases pin reset semantics: clears state immediately + cancels pending timer; safe to call when no timer is in flight (no-op). Bundle: 359.41 -> 359.51 kB.
- **Settings tour-reset pill: migrated to `useFlashState`.** The "Co-pilot UI guidance" reset button flashed a "Reset done" pill for 2 s via a bare `window.setTimeout(setResetFlash(false), 2000)` - the same unmount-leak class the hook was made for. If the user clicked Reset and navigated away within 2 s, the timer fired `setResetFlash(false)` on an unmounted component (React warning). Migrated to `useFlashState(false)` + `flashResetFlash(true, 2000)`. The two parallel patterns in Settings.tsx (`success` and `saveSuccess`) are intentionally NOT migrated this tick because they have a load-bearing pre-API reset (`setX(false)` BEFORE the await) that the hook doesn't directly support without API expansion - keeping their bare-setTimeout pattern documented for a future tick when the hook gains an imperative reset method.

### Fixed

- **CRITICAL: vacuum + prune admin buttons were 403-broken.** `health.ts:vacuumPersistence` and `health.ts:pruneOldJobs` were sending `X-CSRF-Token` while the server's CSRF middleware (server.py:CSRF_HEADER) only accepts `X-Workbench-Token`. So every Compact / Prune click in Settings hit a 403 in production - the user saw "POST /api/jobs/admin/vacuum 403: ..." and the destructive operations they intended never reached the backend. Existing `test_admin_vacuum_requires_csrf` only verified that a MISSING header returns 403; it never exercised a request with a wrong-name header, so the bug shipped silently. Fixed by using the correct header name in both functions, plus a new vitest spec (`health.test.ts`) that pins `X-Workbench-Token` is sent and `X-CSRF-Token` is NOT - so this regression class can't return.
- **Vacuum endpoint: `persistence_unavailable` now returns 503 instead of 404.** The vacuum POST returned 404 for "persistence layer is degraded" while the parallel prune POST correctly returned 503. 404 says "this endpoint never existed" (permanent); 503 says "service unavailable, retry shortly" (transient). The latter is correct for an operational degradation. Aligning the codes also lets a single FE error-handling branch cover both admin endpoints.

### Changed

- **`client.ts:jpost` now uses `fetchWithTimeout`.** Closes the wedged-server-bound sweep gap. Every other mutating module (cli, jobs, settings, demo, annotation) wraps its POSTs with a 30s `fetchWithTimeout` so a wedged server (TCP accepts, never responds) hits an explicit timeout and a recoverable error message - but `client.ts:jpost` (used by `v2.saveRubric` and `v2.submitVerdict`) still called bare `fetch`. submitVerdict is the highest-stakes path through this helper: a reviewer mid-session clicking the verdict button would get a forever-spinner on a wedged server, with no Cancel affordance and no path to recovery without reload. Same 30s bound + same "Server didn't respond within 30s..." message added to keep the surface error consistent with the other mutating modules. Bundle: 359.31 -> 359.41 kB (helper import is shared across modules; the +100 bytes are just timing constants).
- **annotation.ts: stop carrying its own CSRF helpers.** csrf.ts was extracted weeks ago specifically to dedupe annotation.ts's inline `readCsrfToken` + `refreshCsrfToken` (the docstring on csrf.ts says so explicitly: "Originally inline in annotation.ts; extracted here so settings, cli, jobs, and any future mutating endpoint inherit the self-heal behavior without copy-pasting"). All other modules (cli, jobs, settings, demo, client) imported from csrf.ts; annotation never got migrated and kept byte-equivalent local copies. Real reliability cost: any future fix to the CSRF refresh logic (better regex, retry cap, logging hook) would silently miss annotation users until someone noticed the duplication. Now annotation.ts imports from `./csrf` like everyone else - 28 lines deleted, no behavior change. 207 vitest tests pass; bundle size: 359.38 → 359.31 kB.
- **`useFlashState` migration sweep across 4 routes.** Continues the prior two ticks. FailureCluster (`bundleCopy`, `fixCopy`), DatasetDetail (`exportState`), Commands (`copyState` in ShellPreview, `footerCopyState` in CliForm), and RunDetail (`shareState`, `idCopyState`) all migrated - 7 callsites, ~14 bare `window.setTimeout(setX, ms)` calls eliminated. Each block collapses from `setS('copied'); window.setTimeout(...)` to `flashS('copied', 2000)`. The hook supplies unmount cleanup + re-arm cancellation for free, fixing the "Can't perform a state update on an unmounted component" warning class on fast navigation across all 7 surfaces. Also drops `useState` from imports in FailureCluster + DatasetDetail (only consumer was the migrated state). Reports.tsx and AnnotateSession.tsx still use bare patterns - intentionally left for a future tick (Reports has active parallel-session edits; AnnotateSession uses `const t` cleanup which is partially safe). 207 vitest tests pass.
- **CliRunner copy-state migration to `useFlashState`.** Continues last tick's hook rollout. The 4 main copy-state callsites in `OutputSection` (`copyState` for the Copy-output button, `copyIdState` for Copy-ID, `copyUrlState` for Copy-log-URL, `copyCmdState` for Copy-command) plus the `handleCopy` callsite at the bottom each carried the same bare `window.setTimeout(setX, ms)` pattern - 8 setTimeout calls and 5 state declarations across ~80 lines. Migrating to `useFlashState<...>('idle')` collapsed each block from 4 lines (state + setTimeout + setTimeout) to a single `flashCopyXState('copied', 2000)` call. Same defensive semantics gained for free: unmount cleanup + re-arm cancellation. Net diff: 32 lines removed. The remaining 1 callsite (PreviewLine was migrated last tick; CliRunner has no more) is now zero - CliRunner is fully on the hook. 207 vitest tests still pass.
- **New `useFlashState<T>` hook for flash-then-revert UI states.** The "set state, setTimeout to reset" pattern was duplicated across CliRunner (4 copy-state vars + PreviewLine), Reports (copyState), Settings (refreshFlash already uses ref), and a few others - 12+ bare `window.setTimeout(setX, ms)` calls each carrying the same two latent bugs: (1) timer leaks on unmount-during-window navigation, surfacing as "Can't perform a state update on an unmounted component" warnings; (2) rapid double-clicks stack timers, so the FIRST timer's reset can fire while the SECOND flash is still expected to be visible. Both classes of bug fixed by a 30-line `useFlashState` hook that re-arms cleanly + cleans up on unmount, mirroring the `useArmedConfirm` defensive pattern. Migrated `PreviewLine`'s `copyState` as the demonstration site (4 line reduction). 6-case parametrized vitest pin: initial value, flash + revert, rapid re-arm cancels prior, unmount cancels in-flight, second-flash with different transient, latest-initial via ref. Remaining 11 callsites can migrate one tick at a time without churn.
- **Annotate "Customize ranking" hint: fix timer leak on unmount.** The hint pop-over auto-dismissed after 1800 ms via a bare `window.setTimeout(() => setShowRankingHint(false), 1800)`. If the user clicked the trigger then navigated away within 1.8 seconds, the timer fired on an unmounted component and React logged a "Can't perform a state update on an unmounted component" warning. Two clicks within 1.8 s also stacked timers (the second timer's clear of the first was implicit via the state already being true, so the first fire would set the hint false too early). Now the timer ID lives in a `useRef` so each click cancels any in-flight timer before re-arming, and the unmount cleanup `useEffect` clears any timer in flight. Same defensive pattern as `useArmedConfirm`. No customer-visible data behavior change; the leak surfaced only as a dev-console warning during fast navigation.
- **Settings sidebar button: data prefetch now matches NAV_ITEMS pattern.** Last week's tick added bundle prefetch (`preloadSettings`) to the sidebar Settings button so the chunk warmed on hover. But the comment on the NAV_ITEMS table at the top of AppShell explicitly states "Each prefetch fires on hover and warms BOTH the data cache AND the JS chunk in parallel" - and the Settings button only warmed the chunk. So a user hovering 200ms before clicking got the bundle ready but the `/api/settings/redacted` fetch only kicked off post-mount, leaving a brief skeleton flash. Now the hover/focus handlers also call `prefetchV2('settings', () => import('./api/settings').then((m) => m.settingsApi.list()))`, mirroring the `annotation/sessions` warming pattern. The cache key matches what `useV2Resource('settings', settingsApi.list)` uses inside the Settings route, so the route mounts with `isInitialLoad=false` straight away when warmed. Dynamic import keeps `api/settings` out of the main bundle.
- **Metrics "Save rubric": honestly disabled (was UI-lying).** Same dishonesty pattern that the Reports "Send now" fix caught last week, hiding in `routes/Metrics.tsx`. The "Save rubric" button kicked off a 220ms `setTimeout` then flipped a "Saved" pill for 3 seconds - no API call, no persistence, just theater. The user thinks their weight edits are stored; they're not. On refresh the rubric reverts to backend state and the user's tuning vanishes silently. Worse, the post-fake-save `setDraftLoadedFor("local:...")` trick made `dirty=false` so the "unsaved" pill went away, hiding the truth. Now disabled with `title="Rubric persistence is not wired up yet - your edits live in this tab only and will be lost on navigation"` and the label flips to "Save rubric (soon)". The `unsaved` pill is rebranded "unsaved (local)" with a clarifying title and is now governed by `dirty` alone (no more saveStatus games), so the warning persists for as long as the local edits exist. Beforeunload warning preserved (more important now that there is no save path). Removed dead `handleSave` + `saving` / `saveStatus` / `saveError` state + unused `Spinner` and `errorMessage` imports.
- **`EVALYN_LOG_LEVEL` env var now controls the package logger.** Last week's audit-logger fix (`_ensure_app_logger`) hardcoded INFO. That works for default operators, but production operators running the dashboard alongside noisier services often want to suppress the per-mutation INFO chatter (settings save / job spawn / annotation submit etc.) without losing real warnings, and local debug sessions want DEBUG-level traces. New `_resolve_app_log_level()` helper reads `EVALYN_LOG_LEVEL` (case-insensitive, whitespace-tolerant), accepts the standard names DEBUG / INFO / WARNING / ERROR / CRITICAL plus stdlib aliases WARN / FATAL, and falls back to INFO on unset, empty, or unknown values - so a typo can never silently silence the audit trail. 12-case parametrized test pins the resolver behavior including the unknown-string fallback (the bug `logging.getLevelName` returning a string for unknown names is defended by `isinstance(level, int)`).
- **Harmonized "(soon)" suffix on Commands placeholder buttons.** `Run on cluster` and `Schedule` were disabled with `not wired up yet` tooltips but lacked the visible "(soon)" label suffix that the rest of the codebase uses (Reports, Metrics, ExperimentsList). Sighted users had to hover to discover the placeholder state. Now both buttons show "(soon)" inline, matching the canonical placeholder treatment. Same disabled state preserved.
- **Reports "Send now" button: honestly disabled (was UI-lying).** The button flashed "Sent to Slack #ml-quality (placeholder)" via `setState` after a click, but no network request actually fired - the distribution destinations are placeholders. Pure UI lie that erodes user trust. Now disabled with `title="Distribution channels are not wired up yet"` and the label flips to "Send now (soon)" to match the sibling "+ Add destination (soon)" treatment that was already honest. Removed the dead `sendState` / `setSendState` machinery and the `Sent to Slack ...` confirmation pill that depended on it.
- **Pinned `useV2Resource` inflight dedup behavior.** Customer scenario: two components on the same page both call `useV2Resource('home', fetcher)`. The `_inflight` map ensures the fetcher fires once and both components await the same promise. Without the dedup, the server would see N parallel GETs for one logical page load. Test renders two hooks pointing at the same key + same fetcher, waits for both `data` slots to populate, and asserts `fetcher` was called exactly once. Pins the dedup against future regression.
- **Pinned the `useCoPilotThread` error/close-log throttle behavior.** Parallel test to the v2ws throttle test from last tick. Verifies: first `onError` logs once, first unclean `onClose` logs once, subsequent errors/closes within the same outage window (`attempt > 0`) are throttled silent. Catches future regression of the throttle introduced earlier this session in the co-pilot WS reconnect path.
- **Pinned the v2ws error-log throttle behavior.** Added a focused test that uses a `StubWebSocket` global override + a `console.warn` spy to verify: (1) first error in an outage logs once, (2) subsequent errors in the same outage are throttled silent, (3) a successful `open` event resets the throttle so a future outage logs fresh. Catches future regression of the throttle introduced two ticks ago.
- **Sidebar Settings button: hover-prefetch + accessible name + aria-current.** The "⚙ Settings & keys" button at the bottom of the sidebar was the holdout among nav controls - every NAV_ITEMS entry warms its chunk on hover, but Settings didn't. Clicking it forced a chunk-download delay that hovering could pre-warm. Added `onMouseEnter` + `onFocus` -> `void preloadSettings()` so the import races with the click. Also added `aria-label="Open Settings: configure LLM providers and API keys"` (the title attribute alone isn't reliably announced by SR), `aria-current="page"` for the active state, and `aria-hidden="true"` on the decorative ⚙ glyph.
- **Throttled co-pilot WS reconnect noise.** Same pattern as last tick's v2ws fix, applied to `useCoPilotThread`'s outer reconnect ladder. Both `onError` and unclean-close `onClose` previously fired `console.error` on every retry attempt, producing 10+ identical lines per outage (one error + one close per attempt × 5). Throttle uses the existing `attempt` counter: log only when `attempt === 0` (first outage observation); successful `onOpen` / `onMessage` resets the counter so the next outage produces a fresh log line. User-facing `setError` is unchanged - only console noise is suppressed.
- **Throttled v2 WS reconnect noise.** The `/ws/v2/events` stream's `'error'` and `'connect failed'` paths each fired `console.warn` on every retry attempt. A typical dev-server restart (5-attempt reconnect ladder) produced 5+ identical "v2 ws error" lines, burying real errors. Now a module-scoped `_haveLoggedCurrentOutage` flag throttles to one log line per outage window; cleared on successful `open` so a future flake produces a fresh log. Customer-cared less than dev-UX, but the same noise muddied any operator post-mortem of a real connectivity issue. Test reset added to `_resetV2EventStream` for spec isolation.
- **`useVisibilityPoll` dedupes concurrent fetches.** Customer-cared scenario: user mashes the SystemStatusCard "Refresh" button while a fetch is in flight (slow network, or just impatient clicking). Each click invoked the fetcher fresh - N concurrent GETs to `/api/health` for no benefit, plus a small race between the multiple `setValue` / `setLoaded` / `setLastFetchAt` writes. Now the hook holds the in-flight promise in a closure-scoped `pending` ref; concurrent callers reuse the same promise. The `try/finally` clears `pending` on success AND failure so a failed fetch doesn't permanently block subsequent fetches. Same dedup also coalesces back-to-back interval ticks if the fetcher takes longer than the interval (slow server scenario - prevents pile-up). 12 tests cover the existing behaviors + the new dedup pin (rapid refetch mash → 1 fetch, then a fresh fetch after the in-flight resolves).
- **Pinned `delete_old` semantics with 3 new tests.** Following yesterday's fix for boundary-tie behavior, added explicit regression coverage so the fix can't quietly regress: (1) `test_delete_old_keeps_at_least_K_when_boundary_ties` constructs a 5-row table where two rows share the cutoff timestamp and asserts only the strictly-older row gets deleted (1 deletion when `keep=3`, leaving 4 rows = K + tie); (2) `test_delete_old_keep_zero_clears_all` pins the documented "wipe-all" behavior on `keep=0`; (3) `test_delete_old_keep_negative_short_circuits` pins `keep < 0 -> 0 deletes` so a typo in an admin call is safe. Backend suite: 441 passed, 1 skipped, 0 failed.
- **Fixed `delete_old` boundary-tie edge case in JobPersistence.** Subtle: the SQL was `OFFSET keep` then `WHERE started_at_iso <= cutoff`. With ties at the K/K+1 boundary (possible on sub-microsecond job spawns or filesystems with coarse time precision), the `<=` would delete BOTH the K-th and K+1-th rows, leaving fewer than `keep` rows kept. Now uses `OFFSET keep-1` + `WHERE iso < cutoff` so ties at the boundary stay - the function may retain `keep + ties_at_boundary` rows, never less than `keep`. The "keep at least K" semantic is documented in the docstring. Special case `keep == 0` handled explicitly via `DELETE FROM jobs` to preserve the documented "wipe all" behavior (the admin-prune endpoint's existing test pinned this). `keep < 0` short-circuits to 0 deletes (no negative-OFFSET surprise). 35 persistence tests pass.
- **Fixed: cache_invalidate hid the UpdatingChip mid-refresh.** Subtle bug in `useV2Resource`: on a backend cache_invalidate event the hook called `_cache.delete(key)` BEFORE `refetch()`. That made `cached` undefined for the in-flight render, which made `isInitialLoad: !cached` flip to `true`. The UpdatingChip's visibility predicate (`visible={reloading && !isInitialLoad}`) then evaluated to `false` - so the chip HID during exactly the cache_invalidate-triggered background refresh it was designed to indicate. Customer impact: when the FS watcher detects new runs / dataset changes, the user saw the layout sit silently; the chip showed only on user-initiated refetches. Fix: drop the unnecessary `_cache.delete(key)`. The refetch's `_setEntry` overwrites the entry on success, and on failure the prior (stale) cache stays - both better behaviors than wiping it up-front. New test pins `isInitialLoad === false` across a refetch so the bug can't return.
- **Settings "now" tickers snap on tab return.** Two `setInterval(setNow)` patterns in Settings (5s tick for the "Refreshed Ns ago" freshness label under SystemStatusCard, 60s tick for "tested Ns/Nm ago" on the connection-test chip) didn't have the visibility-refresh that `useLiveDuration` got in an earlier tick. setInterval throttles in backgrounded tabs, so a user who left the tab returned to a stale label for up to one tick (5s for the freshness, 60s for the tested-ago label). Both effects now install a `visibilitychange` listener that immediately re-reads `Date.now()` on hidden -> visible. Cleanup on unmount removes both the interval AND the listener.
- **Review cluster items are now keyboard-accessible.** Each row in `ClusterItemList` was a `<div onClick={...}>` - a mouse-only target. Tab skipped past it, Enter/Space did nothing, and only the nested checkbox was focusable for keyboard users. The row's primary affordance ("click to select this item for review in the right pane") was unreachable from the keyboard. Now each row carries `role="button"`, `tabIndex={0}`, an `onKeyDown` that handles Enter and Space (with `preventDefault` on Space to suppress page scroll), `aria-pressed={isActive}` for the selected state, and an `aria-label="Select item <id> for review"`. The nested checkbox keeps independent focus + e.stopPropagation so a single click doesn't fire both the row and checkbox.
- **Final timeout sweep: demo / annotation / agent mutations.** Closes the wedged-server-bound sweep across the last 4 ticks. Demo load (one-shot from Welcome card) gets a 30s window. annotation's shared `mutating` helper covers create-session / verdict / finalize / abandon in one edit. The legacy `frontend/src/api.ts` agent helper (startThread / sendMessage / confirmTool) gets timeout on mutations only - GETs preserved unchanged since they don't lock up UI buttons on hang. Cumulative 4-tick sweep: every user-action mutation in the dashboard - Run, Cancel, Cancel-all, Re-run, Bulk Re-run, Vacuum, Prune, save provider, test connection, set active, delete provider, demo load, annotation create / verdict / finalize / abandon, agent start / send / confirm - is now bounded at 30s. A wedged server cannot park a user on any high-stakes click.
- **Extended `fetchWithTimeout` to admin + settings POSTs.** Continues the wedged-server-bound sweep from the last 2 ticks. Vacuum and prune (Settings page Compact / Prune buttons with armed-confirm spinners) had the same forever-disabled-button risk on a hung server. Settings save / test / active-provider / delete all flow through one shared `jpost` helper, so wrapping that one call site with `fetchWithTimeout` covers every settings mutation simultaneously: provider key save, `/test/{provider}` connection probe, `/active` provider switch, `DELETE /{provider}` credentials removal. All bounded at 30s with the same "Server didn't respond within 30s..." message. The /test endpoint's normal 400-with-body shape is preserved (timeout only fires on actual TCP hang, not on a quick 4xx). Across 3 ticks the sweep now covers Run, Cancel, Cancel-all, Re-run, Bulk Re-run, Vacuum, Prune, save provider, test provider, set active, delete provider — every high-stakes user mutation in the dashboard.
- **Extracted `fetchWithTimeout` helper, applied to `cancelJob` + `restartJob`.** Last tick I added a 30s timeout to `runCli` (Run button); `cancelJob` and `restartJob` had the same wedged-server hang risk - if the server is wedged mid-Cancel-click, the user is stuck with `cancelling=true` and a disabled button forever. Refactored the timeout into a shared `fetchWithTimeout(url, init, timeoutMs, timeoutMsg)` helper that composes any caller-provided AbortSignal with an internal timer; AbortError is mapped to a clear "Server didn't respond within 30s..." message, other errors pass through unchanged. Three callsites (`runCli`, `cancelJob`, `restartJob`) now use the helper - so the Run button, single-row Cancel, Cancel-all (uses `cancelJob`), single-row Re-run, and Bulk Re-run (uses `restartJob`) are all bounded. 4 helper tests + 3 cli tests cover success, timeout, network passthrough, external-signal honoring.
- **`runCli` (the Run-button POST) now has a 30s hard timeout.** Without this, a wedged server (TCP accepted but never responds) leaves the user with `submitting=true` -> Run button disabled forever, no Cancel affordance, no path to recovery except reload. The Run path is the highest-stakes click in the dashboard so a wedged-server hang there is the worst place to be silent. Now an `AbortController` aborts the fetch after `RUN_CLI_TIMEOUT_MS=30_000`, mapped to a clear "Server didn't respond within 30s. The dashboard may be wedged - try reloading." surface (instead of a raw "Failed to fetch" or infinite spinner). Other errors (CSRF 403 retry, network failure, capacity 503) pass through unchanged. 3 unit tests cover: timeout fires the user-facing message, non-AbortError rejections pass through, and the fetch init carries an AbortSignal.
- **Audit logs now actually reach stdout.** Real reliability gap discovered after the 5-tick audit-log sweep: every `logger.info("audit:...")` call in `api/*` was silently going nowhere. Uvicorn only configures its own `uvicorn.*` loggers, the `evalyn_dashboard` package logger had no handler, and Python's `lastResort` only surfaces WARNING+. So a server.py operator running the dashboard saw zero audit lines in their stdout despite having added them in jobs admin, agent, cli, settings, demo, promote, annotation, threads, and rubrics. Now `_ensure_app_logger()` runs at module load, attaches a `StreamHandler` to the `evalyn_dashboard` package logger at INFO level with `"%(asctime)s %(levelname)s %(name)s: %(message)s"` formatter, and idempotently no-ops on re-entry (test harnesses that build_app multiple times). One unrelated test (`test_chat_reuse_thread_does_not_emit_audit_log`) needed `caplog.clear()` between the two chats since it had been "passing" only because INFO records were getting silently filtered before reaching caplog.
- **Recent Jobs drawer rows get list semantics + day headers become real headings.** Continues the list-semantics sweep. The drawer renders interleaved `<DayHeader>` ("Today", "Yesterday", date strings) and `<JobRow>` siblings - SR users heard a flat sequence of unstructured cards. Now the rendered group is wrapped in `role="list" aria-label="Recent jobs - N items"`, JobRow's root carries `role="listitem"`, and DayHeader gets `role="heading" aria-level={3}` so structural breaks between job groups are exposed as proper headings inside the drawer (the drawer itself is `role="dialog"`, so level=3 nests beneath the dialog title without conflicting). Empty states (`<EmptyState>`, `<FilterEmptyState>`) stay outside the list wrapper - they're status cards, not list items.
- **Datasets coverage grid gets list semantics.** Continues the list-semantics sweep (run-detail, failure-cluster, experiments lineage). The Datasets route's main grid rendered N CoverageCards via `.map()` inside a `display: grid` div with no list ARIA. Now the grid carries `role="list" aria-label="N datasets"` (count + pluralization) and each CoverageCard wrapper gets `role="listitem"`. The semantic and the visual layout are independent: `display: grid` continues to control the 2-column layout, `role="list"` exposes it as a list to AT. Empty-state ("No datasets match") rendered separately above this block, correctly stays outside the list.
- **ExperimentsList lineage stream gets list semantics.** Continues the a11y-list-semantics sweep from parallel tickets (run-detail failed-items, failure-cluster items + trigger phrases). The lineage column rendered N LineageNodes via `.map()` inside a plain `<div>` - SR users heard each card as an unstructured element with no list announcement, no count, and no list-shortcut nav. Wrapped in a `role="list" aria-label="N experiment runs"` div (count + pluralization included) and added `role="listitem"` to LineageNode's root. Empty-state Card unchanged - the "No runs match" view doesn't need list semantics.
- **CommandPalette cache now invalidates on backend `cache_invalidate` events.** The Cmd+K palette has its own module-level cache (`_paletteCache`) parallel to `useV2Resource`'s. The hook's cache subscribes to the v2 cache_invalidate WS broadcast and refetches; the palette's didn't, so after running an eval the new run wouldn't appear in Cmd+K until page reload. Same for new datasets and rubric edits. Now a module-level listener subscribes to the same WS stream and nulls the relevant cache slot on matching keys (`experiments`/`experiment` -> runs; `datasets`/`dataset` -> datasets; `rubrics`/`rubric` -> rubrics). The next palette open re-runs the lazy-load path automatically. Active palette state isn't yanked out from under an open palette - the refresh kicks in on next Cmd+K. CLI catalog cache stays cached across invalidates (build-time data).
- **Routed remaining `String(e)` callsites through `errorMessage()`.** Found 6 holdouts that bypassed the centralized helper added a few ticks ago: 4 in CommandPalette (commands / runs / datasets / rubrics fetcher errors -> `errors.push(\`commands: ${String(e)}\`)`), 2 in Settings (BulkTest per-provider results + per-card Test connection result). Same UX consistency bug as the useV2Resource fix - same network failure rendered as "Network unreachable..." in some surfaces and as raw "Error: Failed to fetch" in these. Customer-cared: the BulkTest card's "test all providers" path was the loudest holdout - errors there are exactly when network/key issues are most relevant. Now all 6 sites flow through the helper for the friendly rewrite + clean message extraction. Closes the canonical-error-formatting holdouts; remaining `String()` usages are the helper's own fallback (`errors.ts:97`) and a `new Error(String(err))` constructor pattern in `jobs.ts:374` which is correct usage.
- **Drawer Bulk Re-run also surfaces non-capacity failures.** The bulk-rerun loop only had a banner for the CapacityError early-break and the partial-cap case. Non-capacity errors (network down, validation, server hiccup) hit the `console.warn` branch, kept iterating, and produced no banner at the end. So a user clicking "Re-run all 5" during a brief outage saw zero rows change with no feedback. Now the loop tracks `otherFailures` count + `firstOtherReason`, and after the loop completes (when the cap-banner didn't fire) surfaces "Re-ran K of N; M failed: <reason>." or "Bulk Re-run failed for all N jobs: <reason>." for all-fail. Capacity path unchanged - it still short-circuits the loop and wins when relevant. Closes the fourth silent-failure path in the drawer in this sweep (per-row Cancel, Cancel-all, per-row Re-run, Bulk Re-run all now surface their failures).
- **Drawer per-row Re-run no longer silently falls back to the runner form.** Original behavior: on `restartJob` failure (other than CapacityError) the drawer would `console.warn` then auto-open the CliRunner pre-filled with the job's args and close the drawer. The intent was graceful degradation, but it surfaced as: user clicks Re-run → drawer closes → runner appears with their old args → user has no idea WHY they're staring at a form instead of seeing a job start. Now the drawer stays open and surfaces "Re-run failed: <reason>. Click Re-run again to retry, or open the row for details." in the dismissible action banner. Fewer UI transitions for transient errors, clear next-action for both transient and validation errors. Continues the silent-failure-hunt sweep from the last 2 ticks.
- **Drawer Cancel-all also surfaces partial / total failures.** Same silent-failure pattern as the per-row Cancel I just fixed: bulk Cancel-all uses `Promise.allSettled` to keep one rejection from aborting the rest, then `forEach`-patched only on `fulfilled` and silently dropped rejections. If all 5 cancels failed (network down), the user clicked Cancel-all and watched zero rows change with no banner. Now the handler counts succeeded, captures the first rejection reason, and routes a contextual message to the action banner: all-failed -> "Cancel-all failed for all N jobs: <reason>." Partial -> "Cancelled K of N; M failed: <reason>." All-success skips the banner because the rows flipping to cancelled IS the feedback. Reason text routes through `errorMessage()` so a network failure shows the friendly rewrite.
- **Drawer per-row Cancel now surfaces failures via the action banner.** Previously: user clicks Cancel on a running job, network drops, request fails. The catch block logged to `console.warn` (invisible to the user) and refetched the snapshot. If the snapshot still showed "running", the row stayed unchanged - the user clicked Cancel and visually nothing happened. Now three branches each route to the dismissible action banner: snapshot still running/queued -> "Cancel didn't reach the server: <reason>. The job is still running."; snapshot says notFound -> "Cancel didn't reach the server: <reason>. The job's current state is unknown."; snapshot itself failed -> "Cancel failed: <reason>. Reload to refresh job state." Terminal-status snapshots (job ended via some other path) intentionally don't fire a banner - the terminal state is its own feedback. The error string flows through `errorMessage()` so a network failure shows the friendly "Network unreachable..." text from earlier ticks.
- **Updated stale `v2ws.ts` doc-comment about reconnect backoff.** The header comment said "Auto-reconnect with a fixed 2s backoff" but the implementation actually uses exponential backoff with jitter (added during the WS-resilience tick): 2s -> 4s -> 8s -> 16s -> 30s, plus [0, 500ms) jitter, attempt counter reset on successful open. Comment now reflects the actual ladder so the next person editing this file isn't misled by old text.
- **Reports download filename now reflects the report period, not the download timestamp.** A code-comment-vs-code drift bug: `handleDownloadMarkdown` had a comment saying "Filename includes the report period (when available)" but the actual code used `Date.now()`. So a user downloading three weekly reports today got three files all named with today's timestamp - indistinguishable on disk. Now slug-ifies `report.week_label` ("Week of May 7, 2026" -> "week-of-may-7-2026") for the filename stem; falls back to the download timestamp only when `week_label` is missing so the no-label case still produces unique filenames. Customer-cared: easier to attach the right week to a wiki / archive without re-downloading to confirm.
- **Three a11y gaps closed in AnnotateSession header / output controls.** (1) The "Stats ▴/▾" disclosure button got `aria-expanded={showStats}`; the visual ▴/▾ glyph alone wasn't AT-readable. (2) The "⋯" session-settings menu button (which discloses a `role="menu"` element) got the full menu-button pattern: `aria-label="Session settings"` (the bare ⋯ glyph gives AT users no useful name) + `aria-haspopup="menu"` + `aria-expanded={showSettings}`. (3) The "Diff vs expected" two-state filter toggle got `aria-pressed={showDiff}` - this one is a filter, not a disclosure, so `aria-pressed` is correct per WAI-ARIA (the visible ✓ glyph alone doesn't expose state to AT). Each ARIA attribute matches the right pattern for that button's semantic - disclosure (`aria-expanded`) for the stats panel, menu-button (`aria-haspopup` + `aria-expanded`) for the kebab, two-state toggle (`aria-pressed`) for the filter.
- **Co-pilot dock toggle in sidebar gets proper ARIA disclosure semantics + accessible name.** The button had no `aria-label` and no toggle ARIA, so in the icon-only sidebar state (where the visible "Hide / Show co-pilot" text disappears) screen readers heard just "button". Added `aria-label={dockOpen ? 'Hide co-pilot' : 'Show co-pilot'}` (always set, works in both icon and full sidebar states), `aria-expanded={dockOpen}` (matching the disclosure pattern already used by AppShell's other section triggers - jobs drawer, command palette, help overlay), and `aria-hidden="true"` on the decorative ◑ glyph so AT doesn't announce "circle with right half black" before the meaningful label.
- **CliRunner close button: aria-label mirrors the contextual title for screen-reader parity.** The title attribute already changed to "Close panel (job keeps running - re-attach from Recent Jobs)" when a job is mid-flight, but `aria-label` stayed as the static "Close command runner" - so screen-reader users heard nothing about the background-job behavior and could reasonably assume × / Esc cancelled the running eval. Now `aria-label` flips to match the title's intent: "Close command runner panel - job keeps running in the background, re-attach from Recent Jobs". Both labels read off the same `status === 'running' || 'queued'` predicate so they can't drift.
- **Audit logs added to `/api/threads/save` and `/api/v2/rubrics/{id}` (final 2 mutation gaps).** Closes the audit-log sweep that ran across the last 4 ticks. Thread save: `thread saved: id=<safe-id> messages=N` (no message bodies - users can paste API keys / PII into chats). Rubric save: `rubric saved: rubric_id=<id> fields=name,weights,dimensions` (lists which fields the user touched; None means "kept previous", excluded). Cumulative across 4 ticks: `/api/settings/*` (4 routes), `/api/demo/load`, `/api/promote/run-failures`, `/api/v2/annotation/*` session lifecycle (3 routes), `/api/threads/save`, `/api/v2/rubrics/{id}`. Every backend mutation route now writes a `logger.info` audit trail with privacy-respecting metadata (ids, counts, field names) and never sensitive values.
- **Audit logs added to annotation session lifecycle (create / finalize / delete).** Closing more of the audit-log gap. `api/v2/annotation.py` had 4 mutating routes with zero `logger.info` calls. Added logs at the 3 session-level transitions: `annotation session created: id=... annotator=... source_kind=... source_id=... items_total=N`, `annotation session finalized: id=... annotator=... items_done=N items_skipped=N merged=N` (merged < items_done flags re-finalize-with-overlap), and `annotation session abandoned: id=... items_done=N items_skipped=N` (so postmortem can ask "abandoned at 5% or 95%?"). The per-verdict route is intentionally NOT logged — that's high-volume telemetry, not audit data; session start + finish bookend the work cleanly. Privacy: only metadata (ids, counts, source refs), never the labels or item contents themselves.
- **Audit logs added to `/api/demo/load` and `/api/promote/run-failures`.** Closing more of the audit-log gap that started with `/api/settings/*` last tick. Demo load is a destructive workspace overwrite (copies the bundled fixture into `.evalyn/`); promote creates a new dataset directory from a subset of an existing run's failures. Both are exactly the kind of state-changing infrastructure actions operators need on record. Demo log: `demo loaded: project=research-v1 into=<path>`. Promote log: `promote dataset created: dataset_name=<name> parent=<source> source_run=<id> requested=N promoted=N skipped=N` - the counts surface silent skips that would otherwise need a meta.json fetch to detect. Both fire AFTER full commit (sentinel / meta.json written) so a partial copy never shows as success in the audit trail.
- **Audit logs added to `/api/settings/*` mutations.** Earlier ticks added `logger.info` audit logs to jobs/agent/cli admin endpoints, but `api/settings.py` was missed - setting/deleting/testing API-key credentials happened with zero log trail, exactly the surface where operators most need visibility for incident review. Now every state-changing action logs: `set_active` ("settings active provider set: provider=anthropic"), `set_provider` ("settings provider upserted: provider=anthropic fields=api_key,model"), `test_provider` ("settings provider tested: provider=anthropic ok=True"), and `delete_provider` ("settings provider removed: provider=anthropic"). Privacy contract held: the upsert log lists which fields were touched but never their values - `api_key`, `model`, and `base_url` values stay off the record.
- **Agent confirm dialog (PlanCard) now guards against double-click.** The Approve / Reject buttons in the co-pilot tool-call confirmation card had no in-flight state. A slow network + double-click sent two `confirmAgentTool` POSTs; the second 4xxs because the `tool_call_id` is already consumed but wastes a round trip and surfaces a confusing error. Internal busy state now tracks which button was clicked: both buttons disable, the clicked one gets `aria-busy={true}` and label flips to "Approving..." / "Rejecting...". Race protection: clicking Reject after Approve (or vice-versa) is also blocked. The state lives only as long as the card itself; on `pending=null` the parent unmounts the card and state is dropped naturally. 4 unit tests cover: triple-click fires once, both-buttons-disabled assertion, Reject-after-Approve race, and label flip on click.
- **Drawer action banner is dismissible and dwells longer on long messages.** The drawer's `actionMessage` banner (used for capacity errors, partial-success summaries, etc.) auto-cleared after a flat 6s with no manual dismiss. For long messages like "Re-ran 3 / 5; queue full (8 / 8 running). Try again in 30s for the remaining 2." that's borderline-too-short to read, AND users who finished reading early couldn't move on. Now: dwell time scales with message length at ~50ms/char, floored at 6s (short messages unchanged) and ceilinged at 14s (so even pathological messages eventually clear without action). Added a small × dismiss button with proper `aria-label` for users who've already read the message and want their attention back.
- **`useV2Resource` errors now flow through the shared `errorMessage()` helper.** The hook used `setErr(String(e))` directly, bypassing the centralized helper that handles network-error rewriting and Error-message extraction. Customer-visible inconsistency: the Settings page error card said "Network unreachable - check your connection..." while the UpdatingChip on the same page might show raw "Error: Failed to fetch". Now both use the same path. Discovered while writing tests an unrelated production bug: the side-effect chain `void p.finally(() => _inflight.delete(key))` propagated rejections as unhandled (`.finally` doesn't catch), spamming "Uncaught (in promise)" in the browser console on every fetch failure. Added `.catch(() => undefined)` to the side chain so the inflight cleanup is silent. 4 unit tests cover network-rewrite, plain-Error message extraction, non-Error throws, and error-clearing on successful refetch.
- **Network-failure errors now read "Network unreachable - check your connection and try again." instead of the raw browser TypeError.** When the user is offline / DNS fails / the server is unreachable, fetch() rejects with a `TypeError` whose message varies by browser ("Failed to fetch" in Chrome, "Load failed" in Safari, "NetworkError when attempting to fetch resource" in Firefox). All 20 callsites that pipe errors through `errorMessage()` (UpdatingChip, retry banners, alert blocks) inherited that jargon-y text. Now `errorMessage` detects the shape (TypeError + known message keywords) and replaces with a friendlier sentence, while leaving plain Errors and unrelated TypeErrors (like `x.foo is not a function`) untouched. 5 new tests cover the 3 browser variants, an unrelated TypeError negative case, and a server Error-with-the-word-"network" negative case.
- **Capacity-error messages now use the server's actual `Retry-After` value instead of "in a few seconds".** `CapacityError` already parses `Retry-After` into `retryAfterSeconds` (defaulting to 5 if absent), but all 3 callsites (CliRunner Run-button error, drawer single-row Re-run, drawer bulk Re-run) ignored the field and hardcoded "Try again in a few seconds". Now they use the precise hint via a new `formatCapacityRetryHint(seconds)` helper: `Try again now` for <=1s, `Try again in 30s` for 2-59s, `Try again in 2m` for >=60s. Customer impact: when the server says retry in 30s the user knows to actually wait 30s instead of polling early and burning the cap. 6 unit tests cover the formatter's 3 branches and the existing `errorMessage` helper.
- **Fixed deterministically-broken `useCoPilotThread` F3 reconnect test.** The test asserted that an unclean WS close schedules a reconnect at the 1s backoff tier, advancing fake timers by exactly 1000ms. But the production code (added during the WS-resilience tick) uses `1000 + Math.random() * 500` jitter, so the actual reconnect timer fires anywhere in [1000, 1500)ms. The 1000ms advance missed the random jitter window and the second `subscribeAgent` call never fired. Fix: stub `Math.random` to 0 in the test so jitter collapses to 0 and the timer fires deterministically at 1000ms; restored in finally so other tests are unaffected. Full FE suite back to green: 170/170.
- **Fixed flaky `test_reviews_files_snapshot_invalidated_on_mtime_change`.** The test seeded a dataset, walked once, added a second dataset, called `os.utime(root, None)` to bump the root mtime, and expected the next walk to find both. But `os.utime(path, None)` sets mtime to "now", and WSL/NTFS filesystems can have 1s+ mtime resolution - if the test ran fast enough, "now" equaled the prior mtime within the same tick, the cache thought nothing changed, and the walk was skipped. Under xdist's 32-worker parallelism the timing varied enough to make this an intermittent flake (passed in isolation, failed in suite). Replaced with `os.utime(root, (old + 2, old + 2))` to set an explicitly-later value independent of FS resolution. Full backend suite now: 438 passed, 1 skipped, 0 failed.
- **Fixed `/api/cli/history` returning 404 due to FastAPI route order.** The static `/history` route was registered AFTER the dynamic `/{cli_id}` route in `api/cli.py`. FastAPI/Starlette match in registration order, so `GET /api/cli/history` was matching `/{cli_id}` with `cli_id=history` and falling through to "unknown cli_id: history" 404. The CLI runner's "used N times this week" badge and per-command-history feature were both silently broken. Moved `/history` above `/{cli_id}` and added a load-bearing comment so the next person editing this file doesn't accidentally re-introduce the bug. Two pre-existing tests (`test_cli_history_empty`, `test_cli_history_filter_by_command`) flip from FAILED to PASSED.
- **Closed remaining `aria-busy` gaps across 5 high-impact buttons.** A grep audit surfaced 7 user-action buttons with `disabled={pending|saving|reloading|finalizing|submitting}` but no `aria-busy`, meaning AT users only heard "dimmed button" without the "in flight" cue: drawer Re-run-all-failures, Annotate Finalize-now / Cancel-finalize / Finish-and-save (3-button confirmation), FailureCluster Retry, Review batch Pass / Fail, Metrics Save-rubric, Settings Retry-load and provider Save / Test-connection. The grep `disabled={.*[Pp]ending|[Ll]oading|ing}` paired with the `aria-busy` cross-check now returns no remaining gaps in user-action buttons.
- **Agent API helpers now self-heal stale CSRF tokens.** The `frontend/src/api.ts` agent fetcher (`startAgentThread`, `sendAgentMessage`, `confirmAgentTool`) was the only mutation surface that did NOT retry once on 403 with a refreshed workbench token - every endpoint in `frontend/src/v2/api/*` already had this. Customer scenario: a tab open across a server restart sent its first co-pilot message and got "403 Forbidden" raw error instead of transparently refreshing. Now the agent path uses the same `readCsrfToken` / `refreshCsrfToken` helpers and the same single-retry idiom. 3 unit tests cover: first-try success attaches the header, 403 -> refresh -> retry succeeds, second 403 surfaces as an error (no retry-loop).
- **`useLiveDuration` snaps the elapsed counter on tab return.** A backgrounded tab can have its setInterval throttled (Chrome can drop background ticks to ~1Hz/min in heavy-throttle mode). Without intervention the user came back to a stale "running 12s" that wouldn't update for up to a minute. Now a `visibilitychange` listener does an immediate `setNow(Date.now())` on hidden -> visible so the counter snaps to the correct elapsed the instant the tab is looked at again. Used by both the CliRunner output header and the Recent Jobs drawer rows. 5 unit tests cover live/non-live, sub-minute formatting, time-advance updates, the tab-return snap, and unparseable timestamps.
- **SystemStatusCard retains the last good snapshot during connection issues.** Previously, a single failed `/api/health` poll (network blip, 5xx, malformed body) caused `fetchSystemHealth` to return null and the card to silently vanish - even after the user had been reading fresh numbers a moment ago. Now we hold the last successful snapshot in a ref and keep rendering it with a "Connection issue: showing the last successful snapshot" banner above the rows. The banner clears as soon as the next poll succeeds. First-load failures still hide the card (no prior data to fall back on). Customer-cared: a momentary network hiccup no longer makes the card disappear out from under someone.
- **CliRunner shows a marker when the client buffer drops earlier lines.** The server already emits a `[server dropped N earlier lines]` marker when its retain-cap kicks in, but the client's own MAX_OUTPUT_LINES=1000 ring-buffer was silently slicing the oldest lines off. A user running a chatty eval (>1000 stdout lines) saw the tail with no clue earlier output had been hidden - the start of a stack trace would just vanish. Now the runner inserts a `[client buffer rolled over: N earlier lines hidden]` marker at the top whenever it slices, with the cumulative count formatted with locale separators. Logic extracted into a pure `mergeLinesWithOverflow` helper covered by 6 unit tests (passthrough, slice + count, strip-own-marker-no-double-count, leave-server-marker-alone, locale formatting, no-overflow-marker-preserved). Reset on every fresh Run / Re-run.
- **ErrorBoundary now shows the React component stack and offers Copy details.** When the dashboard hits an unhandled render error, the fallback page previously showed only `name: message` - the React component-stack (which Route, which Provider) was logged to console but invisible to the user. Now the pre includes `err.stack`, `info.componentStack`, the URL, and the User-Agent in a paste-ready format, plus a "Copy details" button that writes the same multi-section report to the clipboard. Falls back to `execCommand('copy')` for older browsers / non-secure contexts. Customer-cared: when a teammate hits a rare crash, they can copy + paste a complete report into a message instead of trying to retype "something about the experiments page broke."
- **Drawer empty-state message now matches which filter is active.** When the user typed a search query that matched nothing, the drawer told them to "toggle the chip above to show all jobs" - confusing since the failed-only chip wasn't what was filtering. The message now branches: search-only ("No jobs match \"X\". Press Esc to clear."), failed-only chip ("No failed jobs match. Toggle the chip above..."), and both ("...Try clearing the search or the chip above."). The suggested action always matches the cause.
- **Failed-job notifications pin until dismissed.** OS Notification's default auto-dismiss (~5s) is fine for "your eval finished cleanly" but dangerous for "your eval crashed" - if the user is in another window for the wrong few seconds they miss the alert and assume the run is still going. `requireInteraction: true` keeps failure toasts visible until the user actively closes them. Successful jobs keep the polite default. Same Slack-style escalation pattern: routine notices auto-dismiss, important alerts pin.
- **RecentJobsDrawer capacity chip now uses the shared `useVisibilityPoll` hook.** Closes out the visibility-aware-polling dedup. Drops ~45 lines of duplicate start/stop/visibility plumbing from the drawer in favor of the shared hook. `enabled: open` replaces the manual `if (!open) return` guard - the hook short-circuits when disabled (no fetch, listener, or interval). The local `capacity` useState was needless duplication once the hook returned the same shape; renamed via destructure (`{ value: capacity }`) so the existing JSX consumer is unchanged. Both call sites (Settings system status + drawer capacity chip) now share one state machine; future surfaces (e.g. a top-bar saturation indicator) inherit the contract for free.
- **Extracted `useVisibilityPoll` hook + retrofitted SystemStatusCard.** Two near-identical visibility-aware polling implementations had stabilized: SystemStatusCard's 15s `/api/health` poll and the drawer's 5s capacity-chip poll. Both share the same shape (visible-on-mount gate, idempotent start, immediate refetch on tab-return, careful cleanup ordering). New `frontend/src/v2/hooks/useVisibilityPoll.ts` owns the state machine; SystemStatusCard retrofitted as the proof. The `enabled` prop supports the drawer's "only poll while open" gate; when false it short-circuits the entire effect (no fetch, no listener, no interval). The `fetcher` is captured in a ref each render so callers can pass inline arrows or fetchers closing over fresh state without forcing memoization or restarting the interval. The `refetch()` callback identity is stable across renders so the SystemStatusCard's "Compact now" handler can call it without depending on the polling effect's closure. Drawer capacity-chip migration comes in a follow-up tick once the hook is proven via Settings (same one-at-a-time pattern as the useSearchFilter rollout). 9 unit tests cover immediate-mount fetch, interval polls, hidden-pauses, visible-on-return restart-and-refetch, manual refetch outside timer, enabled=false short-circuits, enabled false->true starts polling, unmount stops the interval, and the latest-fetcher-reference contract (which protects callers from inline-arrow stale-closure footguns).
- **CliRunner output filter now uses the shared `useSearchFilter` hook.** Closes out the search-filter dedup across all three call sites: drawer, experiments, and now runner output. Drops ~30 lines of duplicate input/debounce/Esc plumbing. The runner's 150ms debounce (vs the hook's 120ms default) is preserved via the `debounceMs` prop - heavy-output runs benefit from slightly less twitchy refilter. No sessionStorage by design here: each run produces a fresh output buffer so persisting a filter across runs would mis-match against lines that aren't there. Esc handling delegated to the hook's `onKeyDown` since the runner's "first Esc clears, second blurs" semantics are exactly what the hook implements (vs the drawer where Esc-on-empty must close the whole drawer, which is why the drawer kept its window-level handler). The "/" hotkey stays at the window level since it must work even when focus is outside the input. All three sites (drawer, runner, experiments) now share a single state machine.
- **RecentJobsDrawer now uses the shared `useSearchFilter` hook.** Second-half of the search-filter dedup begun this loop. Drops ~45 lines of duplicate input/debounce/persist plumbing from the drawer in favor of the shared implementation. Behavior parity preserved: sessionStorage key (`evalyn:v2:jobsDrawer:searchQuery`) unchanged so saved queries don't reset across the swap; initial mount still synchronously hydrates both `input` and `query` from storage so the first render already filters; debounce at the hook's 120ms default matches the old 120ms; empty input still removes the storage entry; private-mode / quota tolerance preserved. Esc semantics intentionally NOT delegated to the hook - the drawer's window-level Esc listener must close the entire drawer on empty input (not blur), so `onKeyDown` from the hook is not wired and the existing window listener owns that path. Same for the "/" hotkey, which stays window-level so it fires even when focus is outside the input. Drawer chunk shrank to 6.61 kB gzip after dedup. Existing `jobsHistory.test.ts` (11 tests) + the hook's own suite (10 tests) all pass.
- **Extracted `useSearchFilter` hook + retrofitted ExperimentsList.** Three near-identical "search input + debounced lowercased query" implementations have stabilized across the dashboard (Recent Jobs drawer, CliRunner output filter, ExperimentsList run search). All share the same shape: immediate input state, ~120ms debounce to a trimmed/lowercased query, optional sessionStorage persistence, ref + "/" hotkey, two-step Esc (clear then blur). At three call sites the duplication was paying interest. New `frontend/src/v2/hooks/useSearchFilter.ts` owns the state machine + persistence + Esc handling; ExperimentsList retrofitted as the proof. Predicate (which fields to substring-match) deliberately stays at the call site - each surface filters different shapes (jobs by cli_id, experiments by id+name+author+tags, log lines by line text) and a `getFields` callback would be more friction than just keeping the array filter local. `sessionKey` is optional so non-persistent contexts can use the same hook. Empty input clears the storage entry rather than persisting `""`. RecentJobsDrawer + CliRunner kept on hand-rolled versions this tick - they have surrounding logic (pin/unpin, line indexing, day headers) that benefits from one-at-a-time migration to verify behavior parity per surface. 10 unit tests cover debounce, sessionStorage round-trip, clear-on-empty, missing-key no-write, private-mode read/write failure tolerance, Esc clear/blur paths, and non-Esc passthrough.
- **Re-running a pinned job inherits the pin on the new entry.** Customer scenario: user pinned a reference run as a known-good baseline ("don't lose this one"). Re-running it before this change produced a fresh entry with `pinned=false`, so the next history-cap eviction would silently drop the rerun and the user's "keep this around" intent was broken on every re-run. Now both single-row Re-run and bulk Re-run inherit `entry.pinned` onto the upserted new entry. The chain of "the run I keep around" survives re-runs. No-op for unpinned source jobs (the `pinned: true` field is only spread when the source had it).
- **Skip OS notification on `cancelled`.** The user just clicked Cancel - a popup telling them their job got cancelled is at best redundant, at worst spammy. The `NotifyOpts.status` type now narrows to `'complete' | 'failed'` so the contract is enforced at the type level rather than carried in caller comments. The hidden-tab gate inside `notifyJobTerminal` is unchanged.

### Added

- **Audit logs on `POST /api/agent/threads/purge-old` and `DELETE /api/agent/threads/{id}`.** Mirrors the jobs-endpoint audit pattern across the agent surface. The agent module had zero logging at all - operators auditing "what destructive operations happened during the incident?" had no visibility into thread purges or deletions. purge-old log captures intent (`max_age_s`) AND outcome (`removed`). delete log fires AFTER the 404 path so phantom-id deletes don't pollute the log; pinned by an explicit success-then-404 sequence test. chat + confirm intentionally NOT logged (chat is high-volume; not destructive in the forensic sense). With this, the dashboard has uniform audit coverage across BOTH job and agent surfaces' destructive endpoints.
- **Audit log on `POST /api/jobs/{id}/restart` with source -> new_job_id linkage.** Final piece of the audit-trail coverage. Restart spawns a NEW job from the source's metadata; the response body returns only the new_job_id, leaving the source linkage invisible to anything reading the API. The log records the mapping so postmortems can trace restart lineage: `admin restart: source=abc123 cli_id=run-eval new_job_id=def456`. The 503 capacity-rejected path gets a separate "rejected" log entry so the audit trail distinguishes "asked but blocked" from "asked and succeeded". All mutating endpoints (cancel/delete/restart/vacuum/prune) now have audit logs - complete forensic coverage.
- **Audit log on `POST /api/jobs/{id}/cancel` and `DELETE /api/jobs/{id}`.** Closes the audit-trail coverage on the remaining mutating endpoints. Vacuum + prune already had audit logs; cancel + delete are equally destructive and warrant the same forensic trail. Cancel log captures the FINAL status after the SIGTERM/grace/SIGKILL dance, so postmortem queries like "did the user kill this run or had it already finished by the time the click landed?" are answerable - a click on an already-finished job logs `status=complete` not `status=cancelled`. Delete log fires AFTER the 404 path so phantom-id deletes don't pollute the log; pinned by an explicit negative-assertion test (deleting a nonexistent ID emits zero audit lines).
- **INFO log on successful `JobPersistence.vacuum()`.** Closes the audit-trail gap on the shutdown-hook path: the admin-vacuum endpoint shipped its own audit log this loop, but the FastAPI shutdown-hook vacuum produced zero log evidence. Operators querying "did vacuum run on the last clean shutdown?" can now grep `JobPersistence vacuum succeeded at <db_path>` instead of relying on server-restart timestamps as a proxy. The path is included so multi-DB setups can identify which file got vacuumed. Two tests pin the contract: success emits one line; the failure-path (early-return on unreadable DB) does NOT emit a success line - a misleading "succeeded" log on a no-op would defeat the audit trail.
- **Audit log on `/api/jobs/admin/vacuum` and `/api/jobs/admin/prune`.** Both endpoints are destructive (prune) or expensive (vacuum) operations that operators may need to track in postmortems. "Did anyone vacuum during the incident window?" was previously unanswerable - the endpoints succeeded silently with no server-side trace. INFO-level lines now record each invocation: `admin vacuum: before=X after=Y saved=Z` and `admin prune: keep=N deleted=M kept=K`. The prune line records both the requested `keep` (intent) AND the rowcount actually deleted (outcome), so the audit trail captures what the caller asked for AND what actually happened. 2 regression tests pin the log-line prefix and keep-value visibility.
- **Public `persistence` property on `JobManager`.** Four external call sites used to reach into the private `_persistence` attribute via `getattr(jm, "_persistence", None)`, which silently returned None if the attribute were ever renamed - a real refactor footgun (affected endpoints would just stop recording persistence-backed values without any error). A public property documents the contract and pins the access pattern: `server.py` (vacuum hook + `/api/health`), `api/cli.py` (per-cli history), and the centralized `_persistence_for` helper in `api/jobs.py` all go through `jm.persistence` now. The underscore on the field stays as a hint that the storage SHAPE is private. 2 unit tests pin the property contract: returns None when not wired; returns the EXACT instance (`is`) when wired - critical for `stats()` cache identity to stay stable.
- **`Cache-Control: no-store` on `/api/health` + `/api/jobs/stats`.** FastAPI/Starlette doesn't add Cache-Control by default. Without it, both endpoints were vulnerable to: browser heuristic freshness caching (Last-Modified absent -> bounded TTL fallback), corporate proxies caching anything without explicit directives, and CDN edges in front of self-hosted dashboards. Either case would freeze the SystemStatusCard's uptime/running counts OR freeze the drawer's capacity chip on a wrong saturation tier - critical for the "approaching cap" warning to actually warn. `no-store` (strictest) chosen over `no-cache` since these are best-effort polled metrics where a stale value is meaningless. 2 regression tests pin the header on both endpoints.
- **Breadcrumb hover prefetch falls back to `preloadByPath` for paths without nav-item warmups.** Closes the gap on `/copilot` which has a breadcrumb mapping but no `NAV_ITEMS` entry - hover used to no-op there. Now: NAV_PREFETCH_BY_PATH[path] (chunk + data) preferred; `preloadByPath(path)` (chunk only) as fallback. /copilot breadcrumb now warms the CoPilotThread chunk on hover. Both onMouseEnter and onFocus get the fallback.
- **`api/health.ts` imports `csrf` statically (consistency cleanup).** `vacuumPersistence` and `pruneOldJobs` used `await import('./csrf')` inside the function body; csrf is two small functions imported statically by 5 other `api/*.ts` modules so it was already in the main bundle. The dynamic await added a per-call microtask round-trip without saving any bytes. Now matches the rest of the api/* files; per-call await gone; bundle shape unchanged. No behavior change.
- **Server-side heartbeat on `/ws/v2/events`.** v2 events WS was the only one of the three streaming surfaces without server-side heartbeat (jobs_ws + agent_ws used `_ws_heartbeat.spawn_heartbeat`). Idle connections (>60s no activity) got reaped by corporate proxies and cloud LBs silently; the FE's onclose triggered reconnect but the user paid the NAT timeout (60-120s) to detect. Now ping every 25s same as the other handlers. FE typeguard accepts 'ping' frames so they pass through. The `_ws_heartbeat` docstring already CLAIMED v2_ws used it - this commit makes the docstring true.
- **`useCoPilotThread` outer reconnect adds [0, 500ms) jitter.** Fourth and last fixed-deterministic WS reconnect ladder. The hook has its OWN outer reconnect logic on top of subscribeAgent's inner auto-reconnect: when subscribeAgent's 5-attempt budget exhausts, this hook reopens for another 5 attempts. Both layers needed jitter; subscribeAgent got it last tick, and now the outer matches. With this, ALL FE-side WS reconnect paths (v2ws, subscribeJob, subscribeAgent, useCoPilotThread) use jittered exp backoff. A reconnect storm across tabs/threads/jobs is now structurally prevented at every layer.
- **`subscribeAgent` reconnect adds [0, 500ms) jitter.** Closes the third and last instance of the thundering-herd gap on FE-side WS reconnects (v2ws + subscribeJob + subscribeAgent). All three paths now use the same `[0, 500ms)` jitter pattern with the same exp backoff (1-8s for per-thread/job, 2-30s for the events stream). A user with multiple tabs + multiple agent threads + a streaming job no longer hammers the server in lockstep on recovery.
- **`subscribeJob` reconnect adds [0, 500ms) jitter.** Closes the same thundering-herd gap on per-job WebSocket reconnects that the v2ws shipped. The exp backoff (1s → 2s → 4s → 8s → 8s) was already in place; jitter prevents multiple tabs subscribed to multiple jobs from reconnecting in lockstep after server recovery. Same 500ms magnitude as the v2ws jitter for consistency. Lower 8s cap stays (per-job streams benefit from faster reconnect when the user is watching live output).
- **`v2ws` reconnect uses exponential backoff with jitter.** The previous fixed 2s reconnect was fine for dev-server restarts but wasted bandwidth and hammered the server when it was down for real (deploy hiccup, network partition). With many tabs all trying every 2s, recovery created thundering-herd load. Now: 2s → 4s → 8s → 16s → 30s (capped); [0, 500ms) jitter so concurrent dashboards don't reconnect in lockstep. Resets to attempt=0 on successful open so future flakes start at the base delay. 5 unit tests on the exported `_computeBackoffMs` helper pin the doubling, cap, and jitter behavior.
- **Annotate resume buttons prefetch session data + items on hover/focus.** The resume buttons already preloaded the AnnotateSession chunk but NOT the session's API data. Click paid the full chunk + `getSession` + `getItems(limit=200)` round-trip. Now hover/focus also warm both API fetches via `prefetchV2` with cache keys matching what AnnotateSession's `useV2Resource` calls use - so warm resumes feel instant. Cache-key match is load-bearing: a typo would silently re-fetch on click and evaporate the perf win without any error.
- **Audit log on `POST /api/agent/chat/{id}/confirm` decisions.** Closes the audit gap on tool-confirmation approve/reject decisions. Volume bounded by user pace (once per confirmation, not per turn). Logs `agent confirm: thread_id=<id> approved=True tool_call_id=tc-1`; auto-approve sessions get an additional suffix; `args_override` intentionally NOT logged (same privacy rationale as chat - user-supplied content can carry secrets). Logging fires AFTER the accepted=True check so the 409 mismatched-tool_call_id path doesn't log phantom decisions that didn't approve anything. With this, every user-driven decision in the agent flow has audit coverage.
- **Cmd+K palette warms route data + chunk on active-index change.** Palette navigation paid a fresh round-trip for the target route on every Enter. Walking through results with arrow keys was particularly bad - the cursor row could sit highlighted for seconds with no prefetch running, then Enter pays the full latency. Now each entry has an optional `warm: () => void` that fires whenever activeIndex points at it. Driving via activeIndex (not onMouseEnter) covers both paths: arrow-key navigation re-fires the effect each step, mouse hover triggers via the existing setActiveIndex. Run entries warm `preloadRunDetail() + prefetchV2('experiment:<id>')`; dataset entries warm DatasetDetail + dataset data; rubric entries warm Metrics + rubrics list. Command entries skip (open CliRunner overlay; no chunk). `prefetchV2` dedupes by cache key so arrow-key bouncing is free.
- **Audit log on `POST /api/cli/run` (success + 503-rejected paths).** Closes the audit gap on the spawn endpoint - operators querying "what was launched during the incident?" or "did anyone get capacity-rejected?" can now grep server logs. Two log shapes match the restart-endpoint pattern: `cli run: cli_id=X job_id=Y` (success) and `cli run rejected (capacity): cli_id=X running=N max=M` (503). Args are intentionally NOT logged - user-supplied form fields can contain paths, API keys pasted by mistake, private dataset identifiers. Pinned by an explicit "args content must NOT appear" check (the `limit=5` and `format=json` form values verified absent from the log line). Forensic chain stays: cli run log → job_id → JM/persistence per-job records (which already carry args without going through this log).
- **Audit log on chat-creates-thread (low-volume operational signal).** `POST /api/agent/chat` was previously excluded from audit logging (high-volume; every user turn would spam). But the create-thread path inside chat is one event per session, not per turn. Now logs `agent thread created: thread_id=<id>` only when chat creates a NEW thread (thread_id missing or unknown). The user's message body is intentionally NOT logged - prompts may contain sensitive content; the log captures the action, not the content. Pinned by an explicit "the message string must NOT appear in the log line" assertion. Useful for "how many sessions started during the incident window?" / "did anyone use the agent yesterday?" forensic queries. Reuse-thread chats stay silent (verified by negative-assertion test).
- **Clipboard fallback throws on `execCommand` failure (was silent success).** The textarea+execCommand fallback path (used when `navigator.clipboard` is undefined - non-secure contexts, older browsers, file:// pages) ignored execCommand's boolean return value. A failed copy resolved silently, leaving callers showing "Copied ✓" feedback when nothing was on the clipboard. Now matches the modern path's behavior: failure throws so the caller's catch block flips to "Copy failed". 5 regression tests pin the contract including textarea cleanup when execCommand THROWS (defensive against the cumulative-leak case in long sessions).
- **Datasets search gains `/` hotkey + two-step Esc.** Datasets had a search box with hand-rolled Esc-to-clear but no `/` focus hotkey. Page-local listener (not via `useSearchFilter` since Datasets persists query in the URL `?q=...` not session storage). Two-step Esc on the input (clear, then blur) matches the runner-output / items-tab pattern; without the blur step a subsequent `/` press wouldn't re-focus because the input would already be focused with empty content and the window listener short-circuits. With this, the "/ to focus" idiom is now consistent across drawer / runner-output / experiments / items-tab / datasets / (copilot-thread intentionally exempt due to surface coexistence).
- **CoPilotThread filter retrofitted to `useSearchFilter` (5th call site).** The thread sidebar's filter was undebounced, used hand-rolled Esc handling, and had the same 120ms empty-state-flash bug ItemsTab had before its retrofit (clearing via Esc would briefly render "No conversations yet" while the debounced query caught up). Now uses the shared hook with 120ms debounce + two-step Esc + the empty-state branches on the debounced query so the message matches what `filteredIndex` is actually doing. Intentionally no `/` hotkey here - items-tab and runner-output already claim `/`, and the thread sidebar is always visible alongside; another listener would race them.
- **`aria-busy` on drawer Cancel-all + per-row Cancel buttons.** Continuation of the sweep started last tick. Both had `disabled={cancelling*}` for mouse + keyboard but missed the aria-busy signal. SIGTERM/grace/SIGKILL dance can take seconds across many jobs; without aria-busy the AT user heard "dimmed" without the "in flight" cue.
- **`aria-busy` on Settings Compact + Prune buttons during in-flight ops.** Both buttons had `disabled={state === 'pending'}` for mouse + keyboard, but no `aria-busy` - screen-reader users heard "dimmed" without the "in flight" signal. Per primitives.tsx Convention 8, the right pattern pairs `disabled` (external guard) with `aria-busy` (announce the action is running). The state machine already has the internal busy guard (`if (busy) return;` early-returns); now the AT side matches the visual state.
- **`Btn` primitive accepts `aria-label` + wired on RunDetail Share + Re-run.** Btn supported `aria-hidden` / `aria-busy` / `aria-expanded` / `aria-controls` / `aria-haspopup` but not `aria-label`. Glyph-prefixed buttons like "↗ Share" and "↻ Re-run" read poorly to screen readers (the glyph is silent or read as a unicode character name). Now state-aware accessible names: Share announces "Run URL copied to clipboard" / "Failed to copy run URL" / "Copy this run's URL to clipboard"; Re-run announces "Loading re-run form" / "Re-run with the same dataset (<name>)" + picks up `aria-busy={rerunBusy}` so SR users get the standard busy signal during the catalog-load window. Other glyph-prefixed buttons can adopt incrementally; opt-in.
- **Failures tab on RunDetail prefetches first cluster on hover/focus.** Clicking the Failures tab navigates cross-route to `/experiments/<id>/cluster/<firstClusterId>`; without prefetch the click paid for both the FailureCluster chunk download AND the cluster-detail JSON in series. Hover/focus now kicks off both warmups (`preloadFailureCluster()` + `prefetchV2('cluster:<id>:<cid>')`). No-op for disabled tab (no failures), no-op for Summary/Items (their data is already prefetched at page level). Mirrors the LiveStreamingCard / QueueRow / breadcrumb prefetch patterns shipped this loop.
- **Breadcrumb buttons on AppShell prefetch on hover/focus.** Side-nav NavLinks already prefetched on hover, but parent breadcrumbs (e.g. "Experiments" while on a deep `/experiments/<id>` route) didn't - clicking a parent breadcrumb to back out paid a fresh round-trip every time. New module-level `NAV_PREFETCH_BY_PATH` derived from `NAV_ITEMS` gives O(1) lookup; adding a new nav item with a prefetch fn automatically gets breadcrumb-hover prewarming for free, no duplicate registration site to drift. Wired to both `onMouseEnter` and `onFocus` for keyboard parity. Optional chaining no-ops cleanly for paths without a registered prefetch (e.g. `/copilot`).
- **TodaysQueue rows on Home prefetch on hover/focus.** LiveStreamingCard already prefetched, but the high-priority QueueRow attention items (top of Home) didn't - clicks paid a fresh round-trip for the experiment-detail JSON + RunDetail chunk. New `experimentIdFromPath` parser extracts the id from `cta_target` paths matching `/experiments/<id>` (with optional ?query and #hash); `warmupCtaTarget` does the standard `preloadRunDetail()` + `prefetchV2('experiment:<id>')` pair. Wired to both `onMouseEnter` (mouse) and `onFocus` (keyboard Tab) for accessibility parity. Non-experiment paths no-op cleanly. 8 unit tests pin the parser including percent-encoded ids, deeper path segments, prefix-vs-segment correctness, and empty-id rejection.
- **ItemsTab search uses `useSearchFilter` hook + `/` hotkey + 120ms debounce.** Fourth call site of the shared search-filter hook (alongside drawer / experiments / runner-output). Three improvements: (1) `/` hotkey to focus the search input, matching convention; (2) the search was undebounced before, re-filtering the entire visible list on every keystroke - the hook's 120ms debounce gives a small but real perf win on heavy item lists; (3) fixes a subtle 120ms-flash bug in empty-state branching - when the user pressed Esc, `search` cleared instantly but `searchQuery` lagged, causing `visibleItems` to remain filtered while the empty-state branched as if search were inactive, briefly rendering "No items match the current filter" instead of the search-no-match path. Aligning the branch on `searchQuery` makes the message match what the filter is actually doing. Two-step Esc (clear then blur) from the hook.
- **ItemsTab clarifies search is page-local + smarter empty states.** The search placeholder said "Search item id..." with no hint that the search only filters the current page; users hunting for an item on page 5 while on page 1 would type the id, see "No items match", and conclude wrongly. Placeholder now reads "Filter this page by id..." with a title tooltip explaining scope. Three distinct empty-state messages: search-active-no-match (with a "use Prev/Next" hint when other pages exist), true-empty-result (surfaces the active filter name), and defensive page-empty fallback. Clear-search button only renders when search is actually active so it doesn't suggest the search is the problem when the filter is.
- **ItemsTab also prefetches the PREVIOUS page (closes bidirectional gap).** "Did I miss something" back-and-forth scrolling is a real workflow during failure investigation. The next-page prefetch shipped just before this leaves a gap: the user clicks Prev expecting instant rendering and pays a fresh round-trip. Now the effect prefetches both neighbors of the current page (next bounded by `total`, prev bounded by `>= 0`). Back-and-forth oscillation becomes fetch-free after the first traversal.
- **ItemsTab prefetches the next page after the current one resolves.** Paging through a long item list previously paid a network round-trip on every "Next →" click - 500+ items at PAGE_SIZE=50 means 10 serial waits in the typical "scan failures" workflow. Now when page N lands, page N+1 is fetched in the background; the next click hits a warm cache and the row swap is instant. Guarded against the final-page case (no prefetch when `offset + PAGE_SIZE >= total`); cache key matches the actual fetch format so back-paging dedupes correctly via `useV2Resource`. Filter/sort changes reset offset to 0 and the effect re-fires with fresh deps so each context's page 2 also gets prefetched.
- **Inline run-id copy affordance on RunDetail.** Operators routinely need to paste JUST the run id (not the full URL) into a CLI like `evalyn run-detail <id>` or into chat messages referencing a specific run. Until now the only copy affordance was the "Share" button which copies the URL; the run id was static text requiring select-and-copy gymnastics. Now the id is a real `<button>` styled to look like the surrounding meta text - dotted underline on hover (matches the SystemStatusCard "Failures (24h)" click-through pattern), shows `✓ <id>` in pass color for 2s after click. Independent copy state from the Share button so clicks don't cross-contaminate visual feedback. Dynamic aria-label announces the run id so AT users get the same "interactive + copies id" affordance.
- **"Refreshed ✓" optimistic flash on the SystemStatusCard Refresh button.** Clicking Refresh was silent: the "Refreshed Xs ago" label would update to "0s ago" but on fast networks the user could miss the transition entirely. A 1200ms label change to "Refreshed ✓" makes the click feel received. Optimistic by design - decoupled from the actual fetch result, so even a failed/slow fetch shows visual confirmation that the click was registered (the `lastFetchAt` label correctness still depends on the fetch landing, giving users both a "click confirmed" cue and a "data current" indicator). Rapid clicks restart the 1200ms window cleanly via timer-clearing on each click; unmount cleanup prevents stale setState.
- **SystemStatusCard freshness label + manual Refresh button.** The visibility-paused polling could show stale data on tab-return until the next 15s tick lands. Without a freshness indicator, the operator couldn't distinguish "polled 1s ago" from "polled 5min ago and connection died" - both rendered identical numbers. Footer now reads "Polled every 15s. Refreshed 4s ago." with a small "Refresh" button for manual triggers (bypasses the 15s interval; useful right after spawning a job when the operator wants the running-count to reflect it immediately). `useVisibilityPoll` gained `lastFetchAt: number | null` returned from each poll - critically, updated on EVERY resolution not just identity-changing ones, so a polling surface returning the same JSON twice in a row still has a moving freshness indicator. A 5s tick re-renders the relative label without forcing the polling hook to fire more often. `formatRelativeAgo` gained an optional `nowMs` param so the caller can thread its re-render state through (the existing "Last vacuum" callsite continues to fall back to `Date.now()` so behavior is unchanged). 3 new unit tests pin the contract.
- **"Prune old" button on Settings + `POST /api/jobs/admin/prune?keep=N`.** Customer scenario: operator sees "Persisted jobs: 5,238" on the SystemStatusCard and wants to enforce a retention policy without restarting the server. The `delete_old(keep=N)` helper has existed; this exposes it as an admin endpoint + a controlled UI trigger. Backend validates `keep >= 0` (negative = unbounded deletion = footgun), allows `keep=0` (delete all), returns `{ok, deleted, kept}`. Frontend wraps the click in `useArmedConfirm` so first click arms (button flips to red `kind="danger"`, label "Confirm prune?"), second within 4s fires; auto-resets if the user walks away. Default `keep=100` matches the helper's default; operators tighten via the inline number input. Pending/success/error feedback mirrors the vacuum button's state machine. CSRF-protected; same protection model as cancel/restart/vacuum. 4 regression tests cover CSRF requirement, negative-keep rejection, the deleted+kept shape, and the keep=0 boundary.
- **Clicking "Failures (24h)" opens the Recent Jobs drawer with the failure filter pre-applied.** Closes the visibility-action loop: the row that flags trouble now also lets the operator drill in. Click-through gated to `recent_failures_24h > 0` - opening an empty failure-filtered drawer would just confuse, so the row stays passive when healthy. New `jobsDrawerBridge.ts` mirrors the existing `cliRunnerBridge` pattern (tiny module-level emitter; drawer state stays out of the global store). Drawer accepts `initialFailureFilter` + a nonce so a second click with the same filter value still re-applies (without the nonce, React's effect dep-equality would skip re-fire). StatusRow gained an optional `onClick` that promotes the row to `role="button"` with Enter/Space keyboard handlers, a hover background, and a dotted underline on the value; non-clickable rows render unchanged. AppShell's bridge subscriber deliberately ignores close events from the bridge - the drawer's own onClose paths (X / overlay / Esc) cover that case, and subscribing to both would create dueling close paths.
- **`recent_failures_24h` field on `/api/health` + "Failures (24h)" row on SystemStatusCard.** Customer-cared "are things going wrong?" glance metric. The value was already computed inside `JobPersistence.stats()` and cached by the recently-shipped TTL cache, so plumbing it through is essentially free. Server pulls both `total` and `recent_failures` from a single `stats()` call - cleaner shape AND removes a dependency on the cache for correctness (if the cache were ever bypassed, we'd still want one call rather than two). New row renders the count in red when > 0 so a glance flags trouble; zero stays neutral so the row reads as healthy when there's nothing to act on. New optional `valueColor` prop on `StatusRow` lets the failure row flag itself in red without threading colors through every consumer. Falls through to 0 in the degraded path, matching the existing best-effort pattern.
- **"Compact now" button on the Settings status card + `POST /api/jobs/admin/vacuum`.** Customer scenario: dashboard has been up for weeks, the sqlite mirror has grown, and the operator wants to reclaim space without restarting the server (which has been the only way to fire `vacuum()` until now). Button runs the same `vacuum()` call mid-session. Endpoint returns `{ok, before, after, bytes_saved}` so the UI can show the win without a separate health refetch. CSRF-protected (matches cancel/restart/settings writes); self-heals stale tokens on 403 via the existing retry pattern. Three button states: idle / pending (`Compacting…` with spinner) / success (Pill: `Saved 12.4 MB` or `Done (no change)` for already-compacted) / error (red text). After a successful vacuum, the card refetches `/api/health` immediately via a refetchRef so DB size + last_vacuum_at update inline rather than waiting up to 15s for the next poll tick - without that, the user would click Compact, see the success pill, but the "DB size" row would show the stale pre-vacuum value, which would be confusing. Success feedback auto-clears after 6s so the card returns to passive status. 3 regression tests cover CSRF requirement, the before/after/bytes_saved shape + math, and end-to-end propagation to the `/api/health` `last_vacuum_at` field.
- **5-second TTL cache on `JobPersistence.stats()` + write-time invalidation.** `/api/health` is polled every 15s by every dashboard tab; with the new SystemStatusCard adding another caller, multi-tab sessions previously paid 4 SQL aggregates per tab per poll (5 tabs × 4 queries × 4/min = 80 queries/min just for status display). A 5s TTL collapses concurrent polls without showing visibly-stale counts: 5s < 15s health-poll cadence so single-tab pollers always see fresh; 5s > typical multi-tab burst so N concurrent polls coalesce to 1; 5s small enough that "Persisted jobs" counts feel real-time even after a bulk purge. Cache key includes `recent_failure_window_seconds` so callers with different windows don't share slots. Invalidation: `upsert_job` / `patch_status` / `delete` / `delete_old` / `vacuum` clear the cache so row-count-changing writes are visible immediately. `append_output` / `set_output_tails` do NOT invalidate (fire many times per second on hot runs; <=5s staleness on `SUM(stderr_count)` is acceptable for this read). `patch_status` was restructured from early-return-then-fallthrough into if/else so the invalidation lands once for both stderr_count branches - the original shape would have skipped invalidation for the without-stderr_count path, a bug-in-waiting. 5 new tests cover TTL hit / TTL expiry / upsert+delete invalidation / distinct-windows isolation.
- **`last_vacuum_at` field on `/api/health` + "Last vacuum" row on the Settings status card.** Customer scenario: operator opens Settings on a long-running dashboard, wants to know how long ago the sqlite mirror was last compacted. Until now the only way to know was to scroll log output or restart the server. Now the card shows `Last vacuum: 2h ago` (or `never (this session)` when no vacuum has fired). Backend: new `_last_vacuum_at: float \| None` on `JobPersistence`, recorded ONLY on `vacuum()` success - failed attempts don't advance the timestamp because the metric communicates "last successful compaction", not "last attempt". `/api/health` surfaces the field; falls through to `None` in the degraded path (matches the best-effort pattern of the other persistence fields). Frontend: new `formatRelativeAgo(eventAtMs)` helper renders "5s" / "5m" / "5h" / "5d" / "30d+", clamps future timestamps to 0s so server clock skew can't produce negatives, caps at 30d+ since a vacuum that hasn't run in 30+ days means the process itself is overdue for a restart. Typeguard explicitly rejects anything other than `number | null` (e.g. stringified dates) so malformed bodies hide the card rather than rendering "-". 3 backend tests + 7 FE tests cover the None default, success-path advance, failure-path non-advance, future-clamp, and 30d+ overflow.
- **SystemStatusCard pauses polling when the tab is hidden + immediate refetch on tab-return.** Mirrors the drawer's capacity-chip pattern. Without this, the card hit `/api/health` every 15s regardless of tab visibility (pure backend waste while the user is elsewhere) AND showed stale uptime/DB-size for up to 15s after tab-return ("1h 12m" climbing from a wall-clock-wrong value the user can guess is off). Now uses `visibilitychange`: hidden -> stop the interval; visible -> immediate fetch + restart the interval. Initial mount also gates `start()` on `document.visibilityState === 'visible'` so opening Settings in a background tab doesn't fire a request until the user looks. Reliability detail: `intervalId` is closed over in the same effect scope by both `start()` and `stop()` so they share state - avoids the stale-closure bug where stop() could miss the latest handle. Cleanup still sets `cancelled` before `stop()` so any in-flight fetch skips `setHealth` on an unmounted component.
- **System status card on Settings.** Surfaces the new `jobs_persisted` + `jobs_db_bytes` health fields shipped earlier this loop. Without a visible surface, those metrics weren't actually solving the SRE-visibility problem - operators had to curl `/api/health` from a terminal to see them. Now the bottom of Settings has a passive grid showing version, uptime ("3d 4h" / "1h 12m" / "47s"), running/cap, agent threads (open/total), persisted-job count (locale-formatted), and DB size (KiB-base since users cross-reference with `du` and `ls -lh`). Polled every 15s; hidden entirely on fetch failure (matches the never-load-bearing pattern of `fetchJobsCapacity`). New `api/health.ts` module types the endpoint with shape validation; `formatBytes` and `formatUptime` are exported so they can be reused in a future status chip elsewhere. 10 unit tests cover edge cases (non-finite/negative input, the 100-threshold drops the decimal, KB/MB/GB unit promotion, days/hours/minutes/seconds bracket logic).
- **`jobs_persisted` + `jobs_db_bytes` on `/api/health`.** Customer scenario: an SRE running the dashboard for weeks wants to know if the persistence layer is bloating before it pages. The vacuum-on-shutdown shipped earlier this loop reclaims space, but steady-state size between vacuums was invisible until now. New `JobPersistence.db_size_bytes()` helper sums the main sqlite file plus the `-wal` and `-shm` sidecars (matters in WAL mode: writes and DELETE tombstones land in the WAL until checkpoint, so naive `stat(main_file)` would under-count real growth). Health endpoint now reports `jobs_persisted` (row count) and `jobs_db_bytes` (on-disk footprint). Both best-effort: a degraded persistence layer falls through to 0 rather than failing the healthcheck, matching the existing fallback for `running` / `agent_threads`. Two helper-level tests cover the side-effect-free zero-on-fresh-install case (a probe must NOT create the file) and the WAL-summing case. Two health-endpoint tests cover the field shape and the JM-missing fallback.
- **"tested 30s ago" timestamp on the per-provider Test result chip.** The Pass/Fail pill now sits next to a live relative-time label so the user can tell whether the chip reflects a fresh probe or a stale earlier click. Updates every 60s while a result is showing (no interval otherwise - idle pages stay quiet). Sub-minute granularity for the first minute (`just now` → `5s ago` → `30s ago`) since that's the window in which the user is actively waiting and the precision matters; minute / hour / day buckets after. `nowMs` is synced inside `handleTest` so the first render of the chip never shows a stale diff against a mount-time clock. Tooltip on the label has the absolute time for users who want exact wall-clock.
- **"Copy log URL" button in the CliRunner output toolbar.** Power-user share affordance for the new `?download=1` endpoint shipped this tick. One click copies an absolute URL like `https://dashboard.example/api/jobs/abc.../output.txt?download=1` to the clipboard - paste into Slack/email and the recipient gets the log as a save-as download. Absolute URL (uses `window.location.origin`) so the link stays meaningful when pasted across origins. Independent copy state from the other "Copy ..." buttons. Visible only when a `jobId` is set.
- **Help overlay (`?`) lists context-specific shortcuts.** The shortcuts I've shipped this loop weren't discoverable: `/` to focus the drawer search, `Cmd/Ctrl+L` to clear the runner output, `Cmd/Ctrl+Enter` to send a co-pilot message, `Cmd/Ctrl+S` to save annotations. They're now in a second section below the global ones, divided by a thin top-border. Each label includes the surface in parens (`Focus search (Recent Jobs drawer)`) so the binding's scope is obvious without sub-headers. The 5 global shortcuts (`Cmd K` / `Cmd J` / `Cmd ,` / `?` / `Esc`) keep their existing position.
- **"Export CSV" button in the Recent Jobs drawer footer.** Customer-cared data-portability win: drag the local drawer history into a spreadsheet for filtering, sharing with a teammate, or pasting into a postmortem. Columns: `job_id, cli_id, cli_args_json, status, exit_code, started_at_iso, failed_at_iso, duration, stderr_count, pinned`. Row order matches the drawer's newest-first sort. Cells properly RFC-4180 quoted (commas, quotes, newlines escaped). Disabled when history is empty. Uses the same client-side blob-URL pattern as CliRunner's "Download .log" - no server roundtrip, the export is whatever the user currently sees in the drawer.
- **Same WS heartbeat on `/ws/agent/{thread_id}`, plus shared helper module.** Extracted the heartbeat into `api/_ws_heartbeat.py` (exports `WS_PING_INTERVAL_S` + `spawn_heartbeat(ws, lock, disconnected)`) and used it from `jobs_ws` and `agent_ws`. `v2_ws` already implements a client-driven ping/pong pattern so no change there. `jobs_ws` keeps its `WS_PING_INTERVAL_S` re-export so the existing patch-the-attr regression test still works (the authoritative value lives in the helper module). All three streaming surfaces now have uniform idle-survival behavior - long agent threads stuck on user confirmation no longer get reaped by NAT timeouts.
- **WebSocket heartbeat on `/ws/jobs/{id}`.** Long evals that sit in a single LLM call for minutes without producing output were vulnerable to silent connection death: many NATs/proxies (60-120s on cloud LBs, as low as 60s on corporate proxies) close idle TCP connections. Now the server emits `{"type":"ping","ts":<unix>}` every 25s while the connection is alive, keeping it warm. Frame is parsed by the FE's existing JSON path and discarded by the if/else chain (no matching branch) - no client change required. Heartbeat task shares the same `send_lock` as the event-fanout loop so a ping never interleaves into a partial frame, and is cancelled cleanly alongside the reader on disconnect. Configurable via `WS_PING_INTERVAL_S`. One regression test patches the interval to 50ms and verifies a ping arrives within 10 frames on an idle long-running job.
- **`/` hotkey focuses the new CliRunner output filter.** Mirrors the drawer's pattern - press `/` outside any input/textarea and the cursor lands in the filter box. Two-step Escape: first press clears the filter, second press blurs back to the runner body so the parent's existing Escape-closes-runner handler can take over (the input's `e.stopPropagation()` on the first press prevents the window listener from closing mid-clear). Help overlay's context-shortcuts list updated: "Focus search (drawer or output filter)".
- **Free-text output filter in the CliRunner toolbar.** Customer pain: 1000+ streamed lines and browser Ctrl+F doesn't survive scroll/streaming additions cleanly. Now a small `Filter output...` input lives in the output toolbar; visibleLines AND-combines with the stderr-only chip. Substring match (case-insensitive). Debounced 150ms so heavy-output runs don't refilter on every keystroke. `N/M` count chip turns red when zero match (so an empty result is obviously the filter, not a missing job). Hidden when there are no lines yet (no useless empty input). System lines (truncation markers etc) are kept by the kind-filter but participate in the text-filter so a user grep-ing for "ERROR" doesn't get a `[server dropped 50 lines]` mark counted as a match.
- **"Copy log URL" button now appends `&include_meta=1`.** Closes the share-flow loop with the new `?include_meta=1` endpoint shipped this tick. Recipient clicks the pasted link, downloads a log file with a `# job_id / cli / started_at / status / exit_code / scope` header at the top - no need for chat context to tell what run produced it. Backwards-compatible: anyone who already has an old copied URL bookmarked still works (just without the header).
- **`/` hotkey + two-step Esc on the ExperimentsList search.** Mirrors the drawer / runner-output patterns. Press `/` outside any input/textarea and the cursor lands in the experiments search. Esc inside: first press clears the filter, second blurs back to body. Help overlay updated: the `/` entry now reads `Focus search (drawer / output / experiments)` so the broader scope is discoverable.
- **Search filter on the ExperimentsList page.** Customer pain: with 50+ runs in the lineage stream, finding "the run named with-fewshot-v2" or "@daisy's runs from yesterday" required manual scrolling. Now a search input lives next to the saved-view chips. Substring match (case-insensitive) against `id, name, author, tags`. AND-combines with the existing saved-view chip (week / gate / all). Debounced 120ms. `N/M` count chip turns red when zero match. `sessionStorage`-persisted query (key `evalyn:experiments:searchQuery`) so a tab switch doesn't lose the filter, mirroring the drawer pattern. `sessionStorage` (not `localStorage`) is the right TTL: filters clear on tab close, not next week.
- **"Download .md" button on the Reports page.** Companion to the existing "Copy markdown" button. Saves the report body as `evalyn-weekly-<isots>.md` so users can attach it to a wiki page, commit it to a repo, or import it into another doc tool without going through clipboard. Same blob-URL pattern as the CliRunner's "Download .log" and the drawer's "Export CSV". Timestamp in the filename keeps multiple weekly snapshots from clobbering each other in Downloads.
- **Drawer search query persists across reopens in the same tab session.** Customer scenario: user types `compare` to find their compare runs, closes the drawer to look at something else, reopens - the search box was empty before this change. Now it's mirrored to `sessionStorage` (key `evalyn:v2:jobsDrawer:searchQuery`) so the next open within the same tab session restores the query. `sessionStorage` (not `localStorage`) is the right TTL: filters are transient state that should clear on tab close, not carry into next week's session. Best-effort: quota / private-mode failures fall back to in-memory only. Empty input removes the entry rather than persisting `""` (cleaner storage).
- **Capacity chip has a 3rd "approaching cap" warning tier.** Previously the chip went straight from muted slate to ember-at-saturation. Now it shows a yellow "approaching" state when running >= ceil(0.75 * max_concurrent) but still under the cap. Customer-cared for SREs/ops watching saturation BEFORE it bites - the difference between "you have one slot left" and "queue is full" matters in practice. Tooltip text differentiates: `${N} / ${M} running` (normal), `Approaching cap: ${N} / ${M} running` (warn), `At capacity: ${N} / ${M} running. New runs will queue.` (saturated). Computed as a single tier ('normal'|'warn'|'saturated') so future tweaks (different thresholds, additional tiers) are one place.
- **Per-row "↓" log download in the Recent Jobs drawer.** Companion to the server-side `output_url` field shipped this tick. Each terminal row (and live running rows) gets a small download icon that opens `…/output.txt?download=1&include_meta=1` directly. Browser saves the log file with the self-describing `# job_id / cli / started_at / ...` header from the recent backend `?include_meta` work. URL constructed client-side (matches the server's `_output_url_for()` format) so the drawer doesn't need a server roundtrip just to render the icon. Hidden for queued (no output yet) and unknown (backend evicted record - the URL would 404). Click stops propagation so it doesn't also fire the row's "open in runner" handler.
- **Stricter validation on `/api/jobs/recent` filter params.** Two improvements: (a) `?cli_id=foo,bar` (with embedded commas) now returns 400 with a hint pointing at `?cli_ids=foo,bar` - previously this silently filtered for the literal string "foo,bar" matching no row; (b) `?since=Infinity` and `?since=NaN` now return 400 with "must be a finite number" - FastAPI's float coercion accepts these but they're nonsense for a timestamp filter and would attempt a SQL comparison on inf otherwise. Two regression tests cover both cases.
- **`last_event_at` field on agent thread metadata.** Useful for "is this thread stuck?" detection: a thread with `has_pending_confirmation=True` AND a stale `last_event_at` is awaiting user action; one with a fresh timestamp is mid-turn. Without this field, a polling client had no way to distinguish idle from active. Unix epoch float (or `null` when the thread has no events yet, e.g. fresh from `create_thread()`). Surfaces on both `GET /api/agent/threads` (listing) and `GET /api/agent/threads/{id}` (single fetch) since both share `_thread_to_dict`. One regression test covers the empty-thread null case AND the post-turn fresh-timestamp case.
- **`GET /api/agent/threads/{id}/messages` returns the conversation text.** Useful when a client wants the assistant's answers without setting up a WebSocket subscription - e.g. an agent-runner test harness, a CLI tool scripting around the dashboard, or a scheduled report capturing thread snapshots. New `AgentRuntime.thread_messages(id)` public helper returns user + assistant message bodies (system prompt filtered out so the endpoint doesn't leak prompt text). Tool-call structure inside assistant messages is preserved as-is. 404 on unknown thread, 503 when agent_runtime missing (consistent with chat/list/delete endpoints). Three regression tests cover the happy path (after a turn drives a user+assistant exchange), unknown-id, and missing-runtime.
- **`output_url` field on every job in `/api/jobs/recent` and `/api/jobs/{id}`.** Customer-cared API ergonomics: lets a FE render a "↓ log" link per row without reconstructing the `/output.txt?download=1&include_meta=1` convention, and lets external integrations (curl wrappers, Slack bots, log forwarders) paste a share-ready URL without knowing the convention. Returned as a relative URL (e.g. `/api/jobs/abc.../output.txt?download=1&include_meta=1`); the FE prepends `window.location.origin` when sharing externally. New `_output_url_for(job_id)` helper centralizes the format so future tweaks (e.g. switching the share-URL shape) are one-place. Two regression tests cover the field on both list and singular endpoints. Empty `output_url` for persisted rows with missing `job_id` (defensive).
- **`?include_meta=1` on `/output.txt` prepends a self-describing header.** Customer scenario: user downloads a log file, opens it a week later, can't tell which run produced it. Now `?include_meta=1` (off by default - existing curl users see no change) prepends `#`-prefixed comment lines: `# evalyn job log`, `# job_id: ...`, `# cli: run-eval`, `# started_at: ...`, `# ended_at: ...`, `# status: complete`, `# exit_code: 0`, `# scope: stream=stderr, tail=3`. Header is `#`-prefixed so most log tools (`less`, `awk`, `grep`) treat them as comments without intervention. Reads in-memory job state when present, falls back to persisted row, returns empty header when neither (shouldn't be observable since the body itself 404s in that case). Two regression tests cover the off-by-default no-header path and the on-with-filters scope reporting.
- **`?download=1` on `/api/jobs/{id}/output.txt` adds a Content-Disposition header.** Useful for shareable / bookmarkable download URLs: `/api/jobs/abc.../output.txt?download=1` is a permalink that browsers save as a file rather than render inline. Filename derives from the first 8 chars of the `job_id` plus the active filters: `evalyn-job-abcd1234.log`, `evalyn-job-abcd1234-stderr.log`, `evalyn-job-abcd1234-stderr-tail3.log`. The suffix encoding means concurrent downloads of different slices of the same job don't clobber each other in the Downloads folder. Two regression tests cover the no-download / with-download shape and the filter-suffix encoding.
- **"Test all configured" bulk health-check on Settings.** Customer-cared admin scenario: user has multiple providers configured (OpenAI + Anthropic + Ollama) and wants a single click to verify which still work after a key rotation, base-URL change, or model deprecation. New `BulkTestCard` between the active-provider selector and the per-provider list. Iterates configured providers (`is_set=true`) sequentially - rate-limit errors don't cascade across providers, results land top-down in run order which reads naturally, and one slow probe (cold ollama) doesn't visually bottleneck the rest (the spinner says exactly which one is in flight). Per-provider results render as `✓ openai connection ok` / `✗ anthropic rate limit`. Hidden entirely when no providers are configured (nothing to test).
- **"copy" affordance on the form-mode preview line too.** The previous commit added "Copy command" to the post-spawn output toolbar; this completes the story by adding a small `copy` button to the form-mode `PreviewLine` (the collapsed `▸ evalyn ...` line above the form). Restructured `PreviewLine` from a single `<button>` (toggle-only) into a `<div>` containing two siblings: the toggle button (left, expand/collapse) and the copy button (right, click-to-copy). `e.stopPropagation()` on the copy click so it doesn't also toggle expand. Independent local copy state with the same `idle / copied / error` pattern other copy affordances use. Now the affordance to grab the assembled shell command exists from the moment the user fills the form, not only after spawn.
- **"Copy command" button in the CliRunner output toolbar.** Customer-cared scenario: user runs `evalyn run-eval --dataset frosty --metric accuracy --limit 50` in the dashboard and wants to paste the same command into their terminal for a quick rerun. The preview line at the top of the output already shows the assembled command but rendered as text; now a one-click button copies the exact `evalyn ...` string (without the `$` sigil so paste works directly). Independent copy state from "Copy ID" / "Copy output" so each button reports its own success/failure. Visible whenever there's a preview to copy (form mode AND post-spawn output view), so the affordance exists from the moment the user fills the form.
- **Per-thread CoPilot draft persistence on the full `/copilot` route too.** Mirrors the dock implementation: on-mount load, 250ms-debounced save, synchronous clear on submit, thread-switch reload. Same shared `copilotDrafts.ts` storage so a draft typed in the dock is restored when the user expands to the full route, and vice versa - the textarea on each surface reads from the same per-threadId key.
- **Per-thread CoPilot draft persistence.** Composes with the form-drafts pattern shipped earlier this loop. Customer scenario: user types a long question into the dock, accidentally refreshes / closes-and-reopens, draft is gone. Now: every keystroke debounces 250ms and persists to localStorage keyed by `threadId` (or `__new__` for the pre-thread state). On mount the textarea restores the saved draft. On submit the draft is cleared synchronously (avoids a race between the debounced save and the navigate-after-send that would otherwise re-persist a stale draft if `threadId` changes mid-flight). Switching threads loads that thread's draft - one open dock can accumulate one draft per thread without interference. New `copilotDrafts.ts` mirrors `cliRunnerDrafts.ts` (LRU cap of 20 threads, quota-failure-tolerant).
- **`agent_threads` + `agent_open_threads` fields in `GET /api/health`.** Composes naturally with the `running` / `max_concurrent` fields shipped earlier this loop: a single curl now surfaces both job pressure AND agent activity, so an SRE dashboard can alert on stuck agents (`open > 0` for a sustained period) the same way it alerts on saturated job slots. New `AgentRuntime.thread_counts()` returns `{total, open}` (open excludes threads whose `final` has fired). Helper is public so server.py doesn't reach into `_threads` and break under future restructuring. Best-effort: if `agent_runtime` is missing or the inspection raises, both fields report 0 (the healthcheck never fails closed on a degraded agent system).
- **Day-group headers in the Recent Jobs drawer.** A 30-entry history spanning multiple days now reads at a glance: thin uppercase dividers ("★ Pinned", "Today", "Yesterday", "Mar 5", or "Mar 5, 2025" for cross-year) precede the first row of each group. Pinned entries collapse into a single "★ Pinned" header (regardless of run date) since their grouping signal is the star, not the date. Unpinned entries fall into newest-first date groups. Implementation centralized as a `renderRowsWithDayHeaders` helper plus a `dayHeaderLabel` pure function so the grouping logic is testable in isolation if we want unit coverage later.
- **`running` + `max_concurrent` fields in `GET /api/health`.** Useful for SRE dashboards that want to alert on saturation: `running >= max_concurrent` for sustained periods is a signal the cap should be raised or the workload spread. Best-effort - if `job_manager` isn't yet attached (early startup race) or the inspection raises, we report zeros rather than failing the healthcheck. The endpoint MUST stay reachable even when the job system is degraded; that's its whole point. Three regression tests cover the new fields, the idle-zero shape, and the missing-job_manager fallback.
- **Browser notifications when a backgrounded job finishes.** Customer-cared scenario: user kicks off a long eval, switches to Slack / docs / another tab, and currently has to remember to come check. Now: when a job reaches a terminal state AND the dashboard tab is hidden AND the user has granted permission, an OS notification surfaces "run-eval finished (45s, exit 0)". Click the notification to focus the dashboard. New `frontend/src/v2/notifications.ts` wraps the Notification API with feature-detection, hidden-tab gating, per-`job_id` `tag` (so a queued -> failed sequence replaces rather than stacks), and silent-on-success / beep-on-failure. CliRunner's terminal-status handler calls `notifyJobTerminal`. Drawer header shows "Enable notifications" only when permission state is `'default'`; once granted/denied the affordance disappears (re-prompting is browser-blocked anyway). All API calls are wrapped in defensive guards so unsupported browsers (older Safari, embedded webviews) report `'denied'` instead of throwing.
- **"Copy ID" button in the CliRunner output toolbar.** Click copies the active `job_id` to clipboard with a transient `✓ ID copied` confirmation. Useful for filing support tickets, grep-ing server logs, or sharing a specific run with a teammate. Visible only when a job has been spawned (no button before Run, none for resumed-from-history rows whose ID is already there). Independent copy state from "Copy output" so a successful output copy doesn't tint the ID button (and vice versa). Truncated id in the tooltip (`Copy job ID (abc12345...)`); aria-label has the full ID for screen readers.
- **Pin a job to keep it in history forever.** Power-user feature for "I want to keep this reference run around even after I do dozens of new ones". Star button on every drawer row toggles `pinned` on the localStorage entry. Pinned entries (1) survive the 30-entry history cap (cap applies only to unpinned), (2) sort to the top of the drawer regardless of age, and (3) survive "Clear" (it now drops only unpinned; the user has to explicitly unpin before clearing). New `setJobPinned(jobId, pinned)` helper. `JobHistoryEntry.pinned?: boolean` is the persisted field. The pin glyph slot uses ★ (filled, ember) vs ☆ (outline, dim) so toggling never shifts row layout.
- **"Reset" button in CliRunner when the form has been edited.** Complements the new form drafts: with edits persisted, users occasionally want a one-click way to undo and start fresh. Reset rolls values back to the form's opening state (schema defaults + draft + seed - the same priority chain `initialValues` captures) and clears the persisted draft so the next open is a clean slate rather than an immediate restore of what the user just discarded. Hidden when the form is pristine (`isDirty=false`) so a fresh form doesn't carry visual noise. Placed left of the existing Cancel/Run buttons for the standard "destructive-ish action on the left" reading order.
- **Args summary line on every Recent Jobs row.** Closes the visibility gap where 5 `compare` or `run-eval` rows looked identical and the user had to hover each one for the tooltip to disambiguate. Reuses the existing `previewCommand` (with the `evalyn <cli_id>` prefix stripped so it doesn't repeat the row's primary line). Mono font, dimmed, single-line with text-overflow ellipsis - long arg strings stay readable without breaking row height. Hidden entirely when the run had no args (no useless empty line).
- **Text search filter in the Recent Jobs drawer.** Power-user feature for "show me my last `compare` runs" or "find that one with `--limit 200`". Substring match (case-insensitive) against `cli_id` plus a JSON dump of `cli_args`, debounced 120ms so a long history isn't refiltered on every keystroke. AND-combines with the failure filter. `/` focuses the input (skips when focus is already in another input/textarea so it doesn't intercept normal typing). Two-step Escape: first press clears the search, second closes the drawer (matches Spotlight/cmd-palette idiom). Match count surfaces below the input when a query is active so an empty result set is obviously the filter and not a missing job. Search row is hidden when history is empty so first-time users don't see a useless filter box.
- **Tab title shows the cli_id when exactly one job is running.** Existing pattern is `(N) Evalyn` for any active job count. With one job, that's now `(1) run-eval Evalyn` (or whichever cli_id) so a user with multiple browser tabs can tell which command is running where without switching focus. With zero or 2+ active jobs, we fall back to the bare count to avoid arbitrarily picking one to highlight. New `activeJobCliId(entries)` helper in `jobsHistory.ts` returns the single-active cli_id or null. The composition is unchanged: "(running) !failed base".
- **Per-command CliRunner form drafts auto-saved to localStorage.** Customer-cared scenario: user opens the runner for `run-eval`, fills 5 fields, gets pulled into Slack for 10 minutes, comes back to find an empty form. Now: every input change debounces 300ms and persists to a small LRU keyed by `cli_id` (cap 30 commands, oldest evicted). New `cliRunnerDrafts.ts` with `loadDraft`/`saveDraft`/`clearDraft`. Priority order on form init: schema default -> persisted draft -> caller-provided seed -> user edits. Seed wins over draft so explicit intent (deep links, "Re-run with same args" prefill from the drawer) isn't shadowed by stale state from a prior session. Suppressed entirely in resume mode (form not visible). Draft is cleared on successful spawn so the next open is a clean slate rather than restoring last-run's values into what the user might think is a fresh form. Quota / private-mode failures are tolerated silently - drafts are best-effort, never load-bearing.

### Tests

- **Integration test for the VACUUM-on-shutdown lifespan hook.** The standalone `JobPersistence.vacuum()` already had unit coverage but the wiring (`@app.on_event("shutdown")` calling it) didn't. Without an integration test, a future refactor that drops the hook would silently regress disk-space recovery on long-running dashboards. Test uses `TestClient` as a context manager (which fires the full startup + shutdown lifespan) and a counter wrapper around the persistence's `vacuum()` to verify it's called exactly once on exit.
- **Regression test for the agent WS heartbeat.** Mirrors the jobs WS heartbeat test from a few ticks ago. Patches `WS_PING_INTERVAL_S` in the shared `_ws_heartbeat` helper module to 50ms, opens a WS to a freshly-created idle agent thread (no chat post), verifies at least one `{"type":"ping"}` frame arrives within 10 receives. Pins the contract that the shared helper is wired into BOTH `jobs_ws` and `agent_ws` - if a future refactor breaks one without breaking the other, this test catches it.
- **Unit tests for `notifications.ts` permission + visibility gating.** 11 tests pin the three-gate contract: API supported, `permission === 'granted'`, `document.visibilityState === 'hidden'`. Exercises both the unsupported path (jsdom default - no `Notification` global) and the supported path (`vi.stubGlobal` with a mock constructor + permission state). Specific assertions: silent=true for `complete`, silent=false for `failed`, per-`job_id` tag so queued→failed replaces rather than stacks, body string includes `exit N` and `Ns`. Exit-code and duration in the formatted body. 81/81 FE tests pass across 12 files.
- **Unit tests for `cliRunnerDrafts.ts`.** Sibling to copilotDrafts but with deliberately different semantics (`Record<string, unknown>` not string; "empty form" is a valid persisted draft; empty `cliId` is a no-op rather than synthetic shared bucket; `loadDraft` returns null on miss not ""). 13 tests pin those distinctions explicitly so a future refactor that tries to "unify" the two modules has a clear failure signal. 70/70 FE tests pass across 11 files.
- **Unit tests for `copilotDrafts.ts` LRU + per-thread semantics.** Same shape as cliRunnerDrafts (which we don't test yet); both came from the same per-context drafts pattern. 11 tests across 5 describe blocks: round-trip per threadId, the `__new__` pre-thread key (null/undefined/empty all bucket together but don't bleed into named threads), `clearCoPilotDraft` removes the entry entirely + empty-string save also removes, LRU cap evicts oldest at MAX_DRAFTS=20, save promotes a re-saved threadId to most-recently-used, corruption tolerance (poisoned JSON / wrong-shape store both return empty rather than throwing). 57/57 FE tests pass.
- **Unit tests for `jobsHistory.ts` pin / cap / sort semantics.** 11 tests cover the localStorage layer that several recent features (pinning, day headers, history filter) depend on. Specifically: cap drops oldest unpinned but preserves pinned even when pins exceed the cap; `sortNewestFirst` puts pinned above unpinned regardless of age; `setJobPinned` toggles cleanly and is a no-op for unknown ids; `clearJobsHistory` preserves pinned and fully clears when no pins; `activeJobCliId` returns the cli_id only when exactly one job is active. Pure-function tests against jsdom's localStorage - no React. Each test resets the relevant storage key in `beforeEach` so order doesn't matter.

### Performance

- **Forward-migration regression test for composite indexes.** Pins that an existing pre-composite-index `jobs.sqlite` (from before this loop) picks up the new indexes on next startup, not just fresh DBs. Hand-crafts a legacy DB shape (table + only the original single-column index), opens it with `JobPersistence`, asserts all three indexes exist after init. Mirrors the existing `test_persistence_migrates_stderr_count_into_existing_db` test that pinned the column-add migration. Without this test, a future refactor that moved index creation behind a "fresh DB only" guard would silently regress query performance for everyone with an existing DB.
- **Composite SQLite indexes on `jobs(cli_id, started_at_iso)` and `jobs(status, started_at_iso)`.** Without these, a `?cli_id=run-eval&limit=50` filter on a persistence table with thousands of rows would scan in started_at order and discard non-matches. With the composites, SQLite seeks directly to the cli_id (or status) prefix and walks in started_at order. New `_COMPOSITE_INDEXES` tuple, created via `IF NOT EXISTS` so re-running on existing DBs is a no-op. The original single-column `idx_jobs_started_at` stays useful for unfiltered queries and `?since`/`?before`-only windowed queries. Two regression tests: `EXPLAIN QUERY PLAN` confirms the planner picks the composite for filtered+ordered queries; `sqlite_master` lookup verifies all three indexes exist after init.
- **Idle-prefetch the newly-lazy overlay chunks.** Closes the perceived-delay gap from the previous commit. Without this, the FIRST Cmd+K / FAB click would visibly pause for 50-200ms while the chunk fetched. Now an `useEffect` in AppShell schedules `void import(...)` for all three overlays via `requestIdleCallback` (where available, falls back to `setTimeout(0)` for Safari). The chunks land in the browser cache during the post-paint quiet window so the first open is instant. Doesn't compete with initial route render because idle-callback runs only when the main thread is otherwise free. Fire-and-forget: failures are silent because the user-driven open path will surface them via Suspense / ErrorBoundary if the network is genuinely down.
- **Lazy-load CommandPalette / RecentJobsDrawer / CoPilotDock.** All three are overlays - they're either closed at first paint or open in response to a click that's tens of milliseconds away from the chunk fetch. Now wrapped in `React.lazy` + `Suspense` and conditionally mounted only when their open flag flips true. CliRunner stays eager-loaded because its `cliRunnerBridge` import has a side-effect that hooks up the global `openCliRunner()` function used by routes from any surface; making it lazy would require introducing a deferred-bridge abstraction. Vite emits new on-demand chunks (`CommandPalette-*.js`, `RecentJobsDrawer-*.js`, `CoPilotDock-*.js` plus shared helpers like `copilotDrafts`, `useArmedConfirm`, `cliRunnerBridge`) totalling ~20kB gzip that defer until first use. Initial-paint payload drops from ~124kB gzip to ~106kB gzip (-15%). Subsequent opens hit the warm chunk and are instant.
- **`useV2Resource` cache now LRU-evicts at 200 entries.** The paginated key shape `experimentItems:${runId}:${offset}:${filter}:${sort}` can produce hundreds of distinct keys for users navigating through many runs (think 30 runs × 5 page offsets × 4 filters × 2 sorts = 1200 keys). Without a cap, the in-memory cache could grow into MB-of-payload territory in long-lived tabs. New `_touch(key)` (move-to-end on read) and `_setEntry(key, entry)` (set + move-to-end + evict oldest) helpers leveraging Map's insertion-order semantics. All read paths (resource hook + prefetchV2) call `_touch`; all write paths call `_setEntry`. Cap of 200 keeps the working set comfortably above realistic single-session navigation depths while bounding worst-case footprint.
- **Drawer refreshes active rows when the tab returns to visible.** Customer scenario: drawer open, user switches tabs for an hour while a long eval runs, comes back. Before this commit, "running" rows in the drawer stayed stale (the localStorage mirror never reconciled with the server) until the user re-opened the drawer or clicked somewhere. Now: a `visibilitychange` listener on the open drawer re-runs the per-row `fetchJobStatus` reconciliation when the tab returns to visible, so any jobs that finished in the background flip from "running" to "complete" / "failed" automatically. Refactored the existing per-open one-shot into a reusable `refreshActiveRows` (reads `entriesRef.current` so the effect doesn't re-fire per patch). Closed drawer = no listener (the per-open one-shot catches up on next open). Composes naturally with the OS-notification feature shipped this loop: a notification arrives -> user comes back -> drawer is already up-to-date.
- **`JobPersistence.vacuum()` + shutdown hook reclaims disk space.** SQLite's `DELETE` (used by `delete_old` to GC old job rows) only marks pages as free for reuse - the `.evalyn/data/jobs.sqlite` file itself never shrinks. Over hours/days of long-running dashboards, the file accumulates fragmentation and grows beyond the working-set size implied by `persistence_keep`. New `JobPersistence.vacuum()` runs `PRAGMA wal_checkpoint(TRUNCATE)` first (so the WAL's pending writes fold back into the main file before the rewrite, otherwise VACUUM would silently no-op the file size) then `VACUUM` to compact. Wired to a new FastAPI `shutdown` hook on `app`. Best-effort: errors are logged at WARN inside `vacuum()` and never bubble - a misconfigured environment must not block process exit. Two regression tests verify (a) total disk footprint shrinks across `*.sqlite + *.sqlite-wal + *.sqlite-shm` after vacuum following a 200-row delete_old, surviving rows still queryable; (b) vacuum on a never-written DB returns False rather than creating the file or raising.
- **Capacity poll pauses while the tab is hidden.** The `/api/jobs/stats` poll for the drawer's "X / Y running" chip used to fire every 5s for as long as the drawer was open, even with the tab in the background. The chip is invisible when the tab is hidden, so the fetch was burning bandwidth/CPU for no user benefit (especially noticeable on always-open tabs left running for hours). New `visibilitychange` listener pauses the interval when the tab is hidden and fires an immediate refetch + restart when it becomes visible again, so the chip is fresh as soon as the user looks at it. No-op semantics in test environments without `document` (SSR, jsdom-without-tab) - we just leave the interval running there.
- **RunDetail prefetches the Items tab's first page once `detail` lands.** The Items tab does its own fetch on mount; without the prefetch, a user clicking "Items" sees a loading spinner. New `useEffect` in `RunDetail` fires `prefetchV2` for the items list under the same cache key the tab will use (`experimentItems:RUN:0:all:item_id`). Limit is dispatched on `compareActive`: regular ItemsTab uses `PAGE_SIZE=50`; ItemsCompareTab uses `ITEMS_COMPARE_LIMIT=200` per side and prefetches both runs in parallel. `prefetchV2` is idempotent (returns early on fresh-cache or in-flight) and swallows errors so the visiting tab still surfaces failures on its real fetch. Established pattern, mirrors prefetches in Datasets / ExperimentsList / Home / AppShell. Memory note flagged this as the only user-visible fetch waterfall worth fixing.
- **On-disk snapshot of `_reviews_files_for_root`.** Mirrors the existing run-dirs snapshot pattern. The reviews-files walk (`Path.glob("*.jsonl")` per dataset's `reviews/` subdir) was the second-largest cold-start cost: ~803ms over 494 placeholder dataset dirs on WSL/NTFS. It sits on the `/home` and `/review` first-paint via `calibration_suggestions`. Now persists to `.evalyn/cache/reviews_files/<hash>.json` keyed on root mtime, with vanished-file skip on load (defensive) and corrupt-file fallback to walk + rewrite. New `_load_reviews_files_snapshot` / `_save_reviews_files_snapshot` / `_clear_reviews_files_snapshots` helpers mirror the run-dirs trio. The prewarm task already calls `_reviews_files_for_root(root)`, so this lifts the 803ms off prewarm too: a fresh checkout's first-ever boot still walks; every subsequent boot reads the snapshot. Four regression tests cover persist+restore round-trip, mtime invalidation, vanished-file tolerance, and corrupt-snapshot fallback.
- **Cold-start FS walk uses `os.scandir` (one syscall per entry, not two).** The dashboard's run-list cold path walks every ``data/<root>/datasets/*/eval_runs/*`` dir on first request after launch (snapshot cache misses on a fresh checkout, mtime change, etc.). The previous implementation used ``Path.iterdir() + Path.is_dir()`` which issues TWO syscalls per entry: one for the directory listing, one for an extra ``stat`` per child. ``os.scandir`` returns ``DirEntry`` objects whose ``is_dir()`` reads from the directory listing's cached type info (``d_type`` on Linux, ``WIN32_FIND_DATA`` on Windows/WSL-NTFS) - one syscall per entry. New ``_scandir_subdirs`` helper centralizes the pattern; ``list_dataset_dirs`` and ``_walk_run_dirs`` both refactored. The eval_runs presence check is also folded into the same ``scandir`` call (caught ``FileNotFoundError``/``NotADirectoryError``) instead of an explicit ``is_dir()`` precheck, halving the per-empty-dataset stat cost. Memory note (484 placeholder dataset dirs, 3.7s cold on WSL/NTFS) flagged this as the dominant cold-start cost. ``follow_symlinks=False`` matches prior implicit semantics. Five regression tests pin shape, sort order, and tolerance for missing/file-shaped/empty-eval_runs corner cases.

### Added

- **"X / Y running" capacity chip in the Recent Jobs drawer header.** Sources `running` + `max_concurrent` from `/api/jobs/stats` (already exposes both). New `fetchJobsCapacity` helper (returns null on any error so the chip is never load-bearing). Polled every 5s only while the drawer is open to keep idle bandwidth at zero. Suppressed at idle (running=0) and when the cap is disabled (max_concurrent=0); turns ember-colored at saturation (running >= max) so the user can connect a "queue full" banner to its cause at a glance. Tooltip switches between "X / Y running" and "At capacity: ... New runs will queue." `aria-label` carries the same shape for screen readers.
- **Typed `CapacityError` + visible "queue full" messaging on 503.** The reliability cap shipped earlier this loop returned 503 to the API, but the frontend rendered it as an opaque `POST /api/cli/run 503: ...` string. New `CapacityError` class (in a new `api/errors.ts`) parses the structured 503 body (`{running, max_concurrent, error}`) plus the `Retry-After` header so callers can `instanceof`-check and render real UX. Wired into three places: (1) `CliRunner` Run button - shows "Job queue full (X / Y running). Try again in a few seconds." and reverts status to the pre-click state instead of `failed` so the user can immediately retry without first dismissing an error; (2) drawer per-row Re-run - surfaces the same banner via a transient action-message that auto-clears after 6s, and skips the prefill-form fallback (the right action on capacity is to retry, not edit args); (3) bulk Re-run - stops the loop early on first capacity hit (subsequent calls would 503 too, burning the Retry-After window) and surfaces partial-success ("Re-ran N / M; queue full, retry the remaining K"). The banner uses `role="status"` + `aria-live="polite"` for a11y.
- **"Re-run all N" bulk action when the drawer's failure filter is on.** Power-user feature for "my batch broke from a config change, retry them all". Visible only when the failure filter chip is active AND there's at least one failure - the chip and the bulk button live side-by-side so the action is contextual to what's on screen. Iterates the visible failures sequentially (NOT parallel) so the new server `max_concurrent` cap throttles naturally rather than firing N parallel 503s, and to avoid localStorage-upsert races on the same tab. Each individual failure (catalog drift, 409, network blip) is logged to the console and skipped - the loop continues so a partial success is better than an all-or-nothing cliff. After at least one successful restart, the failure filter is dropped so the user lands on the unfiltered view and watches the new running entries appear at the top. The button shows a "Re-running N..." progress label and is disabled while the bulk run is in flight to prevent double-clicks.
- **`max_concurrent` cap on `JobManager` + 503 from `/api/cli/run` and `/api/jobs/{id}/restart`.** Reliability guard: without one, an accidental rapid-fire spawn (a script looping `POST /api/cli/run`, a stuck retry loop) can spawn dozens of subprocesses and OOM the host. Default cap is 16 (a normal interactive session never hits it). New `ConcurrencyLimitExceeded` exception raised by `spawn` BEFORE creating the subprocess; both run paths catch and return 503 with a `Retry-After: 5` header and a body shape `{ok: false, error, running, max_concurrent}` so a UI can render "X / Y running" rather than a raw error string. New `JobManager.running_count()` helper used by the cap check and surfaced in `/api/jobs/stats` (always - independent of persistence) so the same number is observable. `max_concurrent=0` disables the cap (test/dev escape hatch). 95 tests pass across the jobs / api/jobs / api/cli suites including 4 new regression tests (running_count tracks correctly, spawn raises at cap and recovers after cancel, cap=0 disables, API 503 shape).
- **"Rotated: X ago" badge on Settings provider cards.** When `updated_at` differs from `added_at` (i.e., the user has saved the provider after creation - typically rotating their API key or switching model), the existing "Last set:" line picks up a `· Rotated: <relative>` suffix using the new backend `updated_at` field. On a fresh record both timestamps are equal (the backend captures `_utcnow_iso()` once and reuses it) so the suffix correctly stays hidden until there's an actual rotation. Useful for "have I rotated this key recently?" checks at a glance.
- **One-click Re-run in the Recent Jobs drawer.** The Re-run button now hits the new `POST /api/jobs/{id}/restart` endpoint instead of opening the runner pre-filled - one click and the new job is spawned, no second Run click needed. New `restartJob` helper in `api/jobs.ts` mirrors the `cancelJob` retry pattern (auto-refreshes a stale CSRF token once on 403). On success, an optimistic `running`-status entry is upserted into history so the new row appears immediately; the WS / status poll fills in the real outcome as events arrive. On failure (catalog drift, 409 on no-cli_id, network blip) we fall back to the original prefill behavior so the rare "edit args first" affordance stays reachable without a second button cluttering the row. Tooltip updated to "Re-run with the same args (instant)".
- **`POST /api/jobs/{id}/restart` re-spawns a finished job.** Customer feature for "rerun this exact command" without UI navigation. Source job's `cli_id` + `args` are looked up (in-memory first, then persistence), the schema fetched from the catalog, and argv rebuilt via `args_to_argv` (rather than reusing the persisted `cmd` string, which is space-joined and lossy for args with whitespace). Returns `{job_id: <new>}` for the fresh run; 404 on unknown source or stale cli_id (no longer in the catalog), 409 if the source is still queued/running (must wait or cancel first - double-spawning silently is rarely the intent), 409 with a descriptive message if the source has no `cli_id` (legacy job, can't rebuild). Four regression tests cover unknown-id 404, running-source 409, happy-path 200 with new id + carried `cli_id`, and no-cli_id 409.
- **`updated_at` timestamp on `CredentialStore` records.** `added_at` already captured creation time; `updated_at` now advances on every `set_provider` write. Surfaced in `public_view` and the new `GET /api/settings/{provider}` endpoint. Useful for "rotated N days ago" displays in the settings UI and for stale-credential advisories. `set_provider` captures `_utcnow_iso()` once and reuses it for both fields so a fresh record has `added_at == updated_at` (no surprising microsecond drift). Two regression tests verify the field is present on both fresh and existing records, and that updates preserve `added_at`.
- **`GET /api/settings/{provider}` single-provider safe view.** Mirrors single-fetch endpoints from jobs/agent/cli. Reuses the existing `public_view` projection (which never includes plaintext keys) and pulls the matching provider entry; 404 on miss. Useful for deep-link clients that already know the provider name and don't need the full settings shape (`active` + every provider). Two regression tests verify shape parity with `public_view.providers[name]` and that plaintext API keys never surface in the response body.
- **`DELETE /api/settings/{provider}` removes provider credentials.** Mirrors purge patterns from jobs/agent. New `CredentialStore.remove_provider(name)` returns True if a record was removed, False on miss. If the removed provider was the currently-active one, `active` is cleared (rather than picking a random replacement) so the dashboard never points at a now-missing record. Idempotent: a second DELETE returns 404 not 500. Useful for the "log out of provider" UX without forcing a switch first. Three regression tests cover the basic remove + GET round-trip, the active-clearing on delete-active, and the unknown-provider 404.
- **`?q=substring` and `?group=Name` filters on `GET /api/cli`.** `q` is a case-insensitive substring match against id/name/blurb/description (whitespace-only treated as no-filter). `group` is exact match. Both AND-combine. Useful for command-palette / keyword discovery (`?q=run` lands `run-eval` and `list-runs`) and for the natural pairing with the new groups endpoint (click a group chip → fetch only that group's commands). Three regression tests cover the q substring (incl. case-insensitive + whitespace), the group filter (count parity with `/api/cli/groups`), and the AND of both.
- **`GET /api/cli/groups` distinct-groups list with counts.** Returns `[{group, count}]` sorted by count descending (alphabetical secondary). Useful for sidebar nav rendering "Groups (8) · Tracing (4)" chips and at-a-glance "what categories of command exist" overviews. Empty-group commands bucket under `""` so totals match `len(catalog)`. Two regression tests verify the count totals and the desc-by-count ordering.
- **`POST /api/cli/{cli_id}/validate` preflight endpoint.** Validates args against the schema *without spawning*. Body `{args: {...}}`, returns `{valid: bool, errors: list[str]}`. Surfaces ALL errors at once (missing-required + unknown-flag in the same response) so a form can highlight every bad field at a glance, distinct from `POST /api/cli/run` which 400s on the first violation. 404 on unknown cli_id. Refactor: extracted `_collect_validation_errors` non-raising helper from `_validate_args` so the validate endpoint and the run path share validation logic. Four regression tests cover happy path, missing-required, unknown-flag, and unknown-cli 404.
- **`GET /api/cli/{cli_id}` single-schema fetch.** Returns one CLI's JSON shape (same as one entry in `GET /api/cli`); 404 on unknown. Useful for deep-link clients (e.g. `/runner/run-eval`) that already know the cli_id and don't need to download the full 35+ entry catalog. Saves bandwidth and avoids parsing the full payload to find one schema. Two regression tests verify shape parity with the list endpoint and the unknown-id 404.
- **`POST /api/agent/threads/purge-old?max_age_s=N` cleanup endpoint** + `AgentRuntime.purge_old_threads(N)` helper. Drops closed threads whose newest event is older than the cutoff. Only `closed` threads are eligible — an open thread (mid-turn or awaiting confirmation) is never purged out from under its own loop. Returns `{removed: N}`. Without this, `_threads` accumulates indefinitely on long-running daemons. The HTTP endpoint can be wired to a cron-like periodic call or invoked manually from admin tooling. Two regression tests cover the heuristic (only closed-and-old threads drop) and the HTTP shape + 400 on negative `max_age_s`.
- **`GET /api/agent/threads/{id}` single-thread metadata fetch.** Mirrors `GET /api/jobs/{id}`. Returns the same shape as one entry in the list endpoint (`{id, message_count, event_count, closed, has_pending_confirmation}`); 404 on unknown. Useful when a client only knows the id (e.g. a deep link) and wants to confirm the thread still exists before opening a WS subscription. Refactor: extracted `_thread_to_dict` helper inside `AgentRuntime` so list and single-fetch share projection logic — the two surfaces never drift on shape.
- **`DELETE /api/agent/threads/{id}` removes a thread.** Mirrors `JobManager.purge` for the chat domain. Closes any active `/ws/agent` subscribers (their async iterators unblock with `StopAsyncIteration`) and wakes any paused confirmation gate with reject=False so a stuck tool-call coroutine returns cleanly. Idempotent on miss — second DELETE returns 404 not 500. This is admin cleanup for "I'm done with this thread", not a way to cancel a mid-flight turn (the turn loop continues but its `_emit` calls become no-ops on the orphaned thread).
- **`GET /api/agent/threads` lists active agent threads.** The `AgentRuntime` accumulates threads via `create_thread`; nothing previously surfaced what was open. New `list_threads()` method returns lightweight `{id, message_count, event_count, closed, has_pending_confirmation}` per thread. Useful for an admin/debug surface answering "what threads has this runtime accumulated?" or polling for stuck threads (`has_pending_confirmation=true` means a turn is gated on user approval). Bodies of messages and events are NOT included — the full event stream is reachable via `/ws/agent/{thread_id}`. Three regression tests cover empty list, multi-thread metadata shape, and the runtime-missing 503.
- **`?tail=N` filter on `/api/jobs/{id}/output.txt`.** Classic `tail -n N` idiom — return only the last N lines of the assembled output (post-stream-filter, post-divider). Useful for quick "what just happened" peeks: `curl /output.txt?tail=20`. Composes with `?stream` so `?stream=stderr&tail=5` lands exactly the 5 most-recent stderr lines. `tail=0` or negative rejected with 400. Always re-adds a trailing newline so the response stays a well-formed text file.
- **`?cli_ids=a,b,c` comma-separated multi-cli filter on `/api/jobs/recent*`.** Complement to `?cli_id` for "show me recent runs of run-eval, compare, OR analyze". Pushed down to a SQL `IN (...)` clause via `JobPersistence.list_recent(cli_ids=[...])`. When both `cli_id` and `cli_ids` are passed, the more-general `cli_ids` wins (a single id is just a one-element list of itself; the AND interpretation would frequently land empty and surprise migrators). Empty list short-circuits to "no filter". Regression test verifies multi-id, single-id-equiv, empty-list, and the cli_id-vs-cli_ids precedence.
- **`?before` upper-bound filter on `/api/jobs/recent*`.** Complement to `?since` — keeps only jobs whose `started_at < before` (strict less-than). Together they enable windowed queries: `?since=T1&before=T2` returns exactly the jobs in the open interval. Validated: `before` must be `> since` when both are set (400 otherwise — empty windows aren't useful and are a likely client bug). Pushed down to SQL via `JobPersistence.list_recent(before_iso=...)`. All three list variants (JSON, CSV, NDJSON) accept it. Regression test verifies single-bound and windowed-AND behavior.
- **`?stream=stdout|stderr` filter on `/api/jobs/{id}/output.txt`.** Filter the plain-text output to a single stream. Default (omitted) keeps the existing interleaved behavior. Useful for `curl ... | grep` only on stdout, or pulling the error log alone for further analysis without the divider noise. In-memory: chronological order within the chosen stream is preserved. Persisted-only: returns just `stdout_tail` or `stderr_tail` directly, no divider. Invalid value (anything other than `stdout`/`stderr`) returns 400.
- **`GET /api/jobs/{id}/output.txt` plain-text variant.** Companion to the JSON `/output` endpoint. Returns `text/plain` with stdout + stderr interleaved in event-id order (the chronological emit order from the subprocess pipes), so `curl ... | grep error` and redirect-to-file workflows work as expected. For persisted-only jobs the original interleaving is lost (tails stored separately); the response concatenates stdout then a `--- stderr ---` divider then stderr to keep tooling from conflating the two streams. Cap is `2 * MAX_PERSISTED_OUTPUT` so total budget matches the JSON endpoint's per-stream cap.
- **`GET /api/jobs/recent.ndjson` NDJSON streaming variant.** One full job dict per line; same `cli_id` / `status` / `since` / `limit` filters as the JSON endpoint. Returns `application/x-ndjson`, no trailing newline. Useful for `jq -c` pipes, log aggregators (Vector / Filebeat plugins), and any tool that ingests JSON Lines. Same response-shape commitment as the JSON endpoint, just chunked by line. Side benefit: extracted a shared `_collect_recent_rows` helper so the JSON, CSV, and NDJSON variants all share filter+merge+sort logic — one bug fix lands in all three.
- **`GET /api/jobs/recent.csv` CSV export endpoint.** Same query semantics as `/api/jobs/recent` (`cli_id`, `status`, `since`, `limit` filters all supported and pushed down where possible) but returns `text/csv` with a `Content-Disposition: attachment; filename="evalyn-jobs-recent.csv"` header for browser-download UX. Columns: `id, cli_id, state, started_at, ended_at, exit_code, duration, stderr_count`. Useful for spreadsheet workflows ("download my last 30 failed runs"), archive tooling, and CLI pipes (`curl ... | column -ts,`). Two regression tests cover the basic shape and the status filter.
- **`/api/health` exposes version + uptime.** The endpoint used to return `{ok: true}` flat. It now also returns `version` (the FastAPI app version), `started_at` (unix epoch when the process booted), and `uptime_seconds`. Backwards-compatible — `ok` is still there. Useful for external monitors verifying the dashboard restarted recently, for tagging UI bug reports to a specific build, and for "is it up yet?" loops in dev-server scripts.
- **`/api/jobs/recent?since=N` epoch filter.** Filter to jobs whose `started_at > since`. Useful for polling: hand back the previous response's max `started_at` to fetch only the delta. Composes with `cli_id` and `status`. Pushed down to SQL via `JobPersistence.list_recent(since_iso=...)`. Regression test verifies strict-greater-than (boundary timestamp not included).
- **`GET /api/jobs/stats` aggregate endpoint.** Returns `{total, by_status, total_stderr, recent_failures}`. The persisted side does the heavy lifting via `JobPersistence.stats()` (single COUNT/GROUP BY/SUM queries — cheap even with thousands of rows). `?recent_window_s` controls the age threshold for the failure count (default 24h). Falls back to in-memory when no persistence is configured. Useful for at-a-glance dashboard health badges or admin tooling answering "how many failed jobs in the last day?" without paging.
- **`DELETE /api/jobs/{id}` endpoint** for explicit job removal. Removes the row from both the in-memory `JobManager` and the sqlite mirror via the new `JobManager.purge(job_id)` method (and a matching `JobPersistence.delete(job_id)` helper). Returns 200 with `{ok: true, id}` on success, 404 if unknown in both stores, 409 if the job is still queued/running (the user must `POST /cancel` first — we never purge a job whose subprocess is alive because that would orphan the reaper task and the captured streams). Three regression tests cover the happy path, the running-job 409, and the unknown-job 404.
- **`/api/jobs/recent?status=X` filter.** Mirrors `cli_id`. Filter to one of `queued` / `running` / `complete` / `failed` / `cancelled` to fetch e.g. only failed jobs for a regression scan. Both filters compose: `?cli_id=run-eval&status=failed`. Pushed down to SQL on the persisted side via `JobPersistence.list_recent(status=...)`; in-memory jobs are filtered at the API layer. Regression test covers status-only, status+cli_id combined, and unknown-status empty result.

### Performance

- **`JobPersistence.list_recent` pushes `cli_id` filter down to SQL.** The previous tick added the filter at the API layer (post-projection in Python). Now the filter is a `WHERE cli_id=?` clause so installations with thousands of persisted rows do not pay a fetch-and-filter cost. `/api/jobs/recent?cli_id=X` calls the new parameter; `cli_id=None` returns the unfiltered query as before. Added a regression test verifying filter results, no-match returns empty, and unfiltered returns all rows.

### Added

- **`GET /api/jobs/{id}/output` endpoint** returning the captured stdout/stderr tails as JSON. Shape: `{id, state, stdout_tail, stderr_tail, stderr_count, total_chars}`. In-memory jobs build tails from the event log (capped at `MAX_PERSISTED_OUTPUT` per stream); persisted-only jobs read the already-capped tails from sqlite; both miss → 404. Useful for clients wanting the final output without setting up a WebSocket — agents fetching tool-call results, deep links to "show this completed run's logs", external tooling. Two regression tests cover the in-memory tail assembly and the unknown-job 404.
- **`/api/jobs/recent?cli_id=X` filter.** Lets clients query "recent jobs of type X" without fetching everything and filtering client-side. Filter applies to both the in-memory and persisted sources before the merge. Bonus: `_job_to_dict` and `_persisted_to_dict` now both include `cli_id` in their JSON shape (previously only persisted rows had it via the `cmd` array fallback), so frontend code can read `entry.cli_id` directly without parsing `cmd[1]`.
- **`stderr_count` persists across server restarts via the sqlite mirror.** The previous tick added `Job.stderr_count` and exposed it on `/api/jobs/{id}`, but it was in-memory only — a server restart wiped the count. `JobPersistence` schema now has a `stderr_count INTEGER NOT NULL DEFAULT 0` column, with a forward-migration `ALTER TABLE` that runs on first load for older jobs.sqlite installations (existing rows back-fill 0 via the column DEFAULT). `JobManager._persist_job_terminal` writes the count via the new `patch_status(stderr_count=...)` parameter. Bug fix in the same commit: `_readable()` now triggers `_ensure_schema()` when the DB file exists but our schema-ready flag is not set, so forward migrations apply BEFORE the first read (previously a pure-read consumer would see the legacy schema). Two new regression tests cover the round-trip and the legacy-DB migration path.
- **Ctrl+L / Cmd+L clears visible CliRunner scrollback.** Terminal idiom for "clear screen". Drops the `lines` state and the rAF flush buffer but does NOT cancel the running job — the WS keeps streaming new lines onto a fresh slate. The shortcut is intentionally suppressed when the user is typing in a form input/textarea so it does not interfere with browser default behavior. Active only while CliRunner is mounted (the listener installs/removes via the same useEffect as the existing Escape-to-close handler).
- **"Cancel N" bulk button in RecentJobsDrawer footer.** When `activeCount > 0` (queued/running jobs in history), the footer renders a "Cancel N" button next to "Clear history". Two-click confirm pattern matching the existing clear button. On confirm, fires SIGTERM in parallel via `Promise.allSettled`, optimistically patches each successful cancel to `cancelled` (failures leave the row untouched so a stale 404 doesn't paint a wrong status). "Cancelling…" disabled state during the in-flight wait. Complements the per-row ✕ button from a previous tick — that's the surgical option, this is the bulk one.
- **stderr count surfaced in RecentJobsDrawer rows.** `JobHistoryEntry` gained an optional `stderr_count` field. CliRunner counts stderr lines from the raw stream (so the true total survives the visible buffer's MAX_OUTPUT_LINES ring trim) and persists it on every terminal-status patch. Drawer rows now render a "5 stderr" inline chip alongside the exit-code metadata when the count is positive — useful for spotting noisy jobs even when exit code 0. Counter resets on Re-run.
- **Row tooltips in RecentJobsDrawer show the full preview command.** Hovering a row used to surface only the opaque `job_id` ("a8f3..."); now it shows the full argv ("evalyn run-eval --dataset path --metric f1 --limit 100"), so users can disambiguate similar runs without opening them. The `previewCommand` helper that builds this string was extracted from CliRunner into the shared `v2/api/cli.ts` so both surfaces use the same formatting (skip empty/null/undefined, drop false booleans, unfold array values into multiple positionals).
- **Visible "Reconnecting…" pill in the co-pilot dock during agent WS retry.** Mirrors the CliRunner reconnect indicator from the previous tick. `subscribeAgent` gained `onReconnecting` / `onReconnected` callbacks; `useCoPilotThread` debounces them through a 1.5s timer and exposes a `reconnecting` boolean. CoPilotDock renders the same ember-coloured pill in its header next to the turn count when reconnecting is active. Brief blips stay invisible; longer recoveries are surfaced so the user knows a quiet chat is recovering, not hung. Cleanup paths (thread reset, hook detach, max-attempts give up) all clear the timer + state.
- **Visible "Reconnecting…" pill in CliRunner during WS retry.** The auto-reconnect work in earlier ticks made transient WS disconnects invisible — but a long retry window (up to 23s with 5 attempts) could be confusing: the user sees a gap in output and may suspect a hung eval. CliRunner now surfaces a small ember-coloured "Reconnecting…" pill in the output preview row when a disconnect lasts past 1.5s, hiding the cosmetic flash for blips we recover from quickly. Pill clears as soon as the WS reconnects successfully or the user hits Re-run / leaves the runner. Implemented via two new `subscribeJob` callbacks (`onReconnecting`, `onReconnected`); each fires with try/catch so a consumer throw cannot kill the reconnect path.
- **Cancel button on running rows in RecentJobsDrawer.** Symmetric to the Re-run button on terminal rows. Each queued/running row now shows a small `✕` button on the right edge; click sends SIGTERM via the existing `/api/jobs/{id}/cancel` endpoint and optimistically patches the local entry to `cancelled`. Previously, the only way to cancel a still-running job was to navigate back to the CliRunner instance — if that runner was already closed (user moved on), the job ran to completion regardless. The button is disabled with a `wait` cursor while the cancel HTTP is in flight so a double-click does not fire two cancels. On HTTP failure (network down, 404 because the job already finished between click and request), the row falls back to a snapshot refresh so its status reflects whatever the server actually thinks.
- **Re-run button on RecentJobsDrawer rows.** Each terminal job row (complete / failed / cancelled) now shows a small `↻` button on the right edge that opens the CliRunner with the row's args pre-filled but no `resumeJobId`, dropping the user on the form one Run-click away from a fresh launch. Previously the only path was: click row → CliRunner opens in resume mode → click "Re-run" inside. The row layout was refactored from a single `<button>` to a flex container with two sibling `<button>`s (resume + re-run) so the inner button is valid HTML; hover state moves to the wrapper so the whole row still highlights together. Hidden for queued/running rows (a live job already exists) and for the `unknown` evicted state. Falls back to the resume click if the cli_id is no longer in the catalog (re-run needs a real schema to render the form).
- **Tab title surfaces unacknowledged failures.** The AppShell title prefix already shows `(N)` when jobs are running; it now also appends `!N` when N recent jobs failed since the user last opened the Recent Jobs drawer (e.g. `(1) !2 Eval · Evalyn`). Backed by a new `failed_at_iso` field stamped on first status-transition to `failed` and a localStorage `ACK_KEY` timestamp updated whenever the drawer opens. Cross-tab synced via the `storage` event so opening the drawer in one tab clears the badge in others. Lets a user on another browser tab notice an overnight or background-tab eval failure without having to click into the dashboard.
- **Failed-jobs filter chip in RecentJobsDrawer header.** The drawer keeps up to 30 recent jobs in local history; spotting which ones failed used to mean scanning every status pill. A new clickable chip in the header (`X failed`) appears whenever any row's status is `failed` and toggles a "show failed only" filter, mirroring the pattern from the CliRunner stderr chip. The filter excludes `cancelled` (user-driven) and `unknown` (server-evicted) since neither is an unexpected failure. Auto-clears when the failed count drops to 0 so the chip never disappears mid-filter and strands the user with an empty list. A defensive empty-state covers the brief race window between toggle and auto-clear.
- **Jump-to-first-error button in CliRunner output header.** When stderr exists and the filter is off, a small `↥ first` button next to the stderr-count chip scrolls the first stderr line into view (`scrollIntoView` with `block: 'center'`) so the user can locate the first failure point without losing the surrounding stdout chronology. Hidden when the filter is on (the first error is already on screen) or when stderr is empty. Companion to the click-to-filter chip below: filter for "show only errors", jump-to-first for "show me where it started" while keeping context.
- **Click-to-filter stderr in CliRunner output.** The stderr-count chip in the CliRunner output header is now interactive: click it to filter the visible output to stderr (and system) lines only, click again to show all output. The chip's style (filled background, bold weight, leading checkmark) reflects whether the filter is active. A dedicated empty-state shows "No stderr lines yet" when the filter is on and nothing matches, instead of the default "(no output)" which would be misleading. The Copy button continues to copy ALL lines regardless of the filter — the filter is for scanning, the clipboard is for sharing. The filter auto-clears whenever the stderr count drops to 0 (terminal status with no errors, or all stderr evicted from the ring buffer mid-run) so the user can never get stuck with a stale filter when the chip itself is no longer rendered. Lets users find a buried failure in a chatty eval in one click instead of manual scrolling.

### Performance

- **`subscribeAgent` auto-reconnects on unexpected close with `?since=N` cursor.** Symmetric to the prior `subscribeJob` reliability work, applied to the chat WebSocket. Network blips, dev-server restarts, and brief disconnects mid-conversation no longer leave the co-pilot stream dead; the WS retries with exponential backoff (1s → 2s → 4s → 8s → 8s, max 5 attempts) and forwards `?since=lastSeenEventId` so the backend skips events the consumer already saw — no duplicate text-deltas, no missing tool-call cards. The `onOpen` handler fires on first connect only so reconnects do not double-fire it; `onClose` is suppressed for intermediate disconnects we recover from. After 5 failed attempts, `onError` fires with a clear "Lost agent stream after 5 reconnect attempts" message. Bonus: separated the JSON-parse and `onMessage`-throw error paths so the console message accurately names the failure mode (the previous code labelled both as "agent ws json parse error"). Existing callers benefit transparently — no API change.
- **`subscribeJob` auto-reconnects on unexpected close with `?since=N` cursor.** Network blips, dev-server restarts, and brief disconnects mid-stream used to leave CliRunner stuck on a "Lost connection to job stream" toast; the user had to refresh the page to resume tailing. The WS subscriber now retries with exponential backoff (1s → 2s → 4s → 8s → 8s, max 5 attempts ≈ 23s patience) and passes the highest observed `event_id` as `?since=N` so the backend replays only events the client has not yet seen — no duplicate lines, no missing lines. The "running" onStatus pill fires on first connect only, so a reconnect after the user clicked Cancel does not flip the UI back to running. `onClose` is suppressed for intermediate disconnects we recover from, then fires once on terminal close (user-initiated, server-delivered exit, or attempts exhausted). After 5 failed attempts, `onError` fires with a clear "Lost job stream after 5 reconnect attempts" message. Existing callers benefit transparently — no API change.
- **Co-pilot chat text-delta batching via requestAnimationFrame.** The streaming agent reply previously called `setMessages` per token (~80/sec for Sonnet, faster for Haiku), each rebuilding the messages array, the agent bubble, and concatenating bubble text. `useCoPilotThread` now coalesces deltas into a `Map<messageId, accumulatedText>` ref and flushes once per animation frame, so the chat re-renders at most ~60Hz regardless of token rate. Other event kinds (tool_call_*, final, error) pass through directly. The `final` event force-flushes pending deltas before applying its patch so the bubble's text is current and never "rewinds" mid-stream. WS reconnect, thread reset, and unmount all cancel pending rAF and clear the buffer to prevent stale tokens leaking into a new conversation.
- **CliRunner WS line batching via requestAnimationFrame.** Chatty eval runs (100s of stdout lines/sec) used to call `setLines` once per WebSocket message, triggering one React render per line. CliRunner now buffers lines in a ref and flushes once per animation frame (`requestAnimationFrame`), capping render rate at ~60Hz regardless of line rate. The buffer is bounded at `MAX_OUTPUT_LINES` so a backgrounded tab cannot accumulate unbounded memory while rAF is paused. Cleanup on unmount and Re-run cancels any pending frame and clears stale lines so they do not bleed into a new run. No visible behavior change for slow streams; high-rate streams stop juddering.

### Changed

- **RecentJobsDrawer's "show failed only" filter is now persistent.** The chip state lives in localStorage under `evalyn:v2:jobsDrawer:failureFilter`, so a user who toggles "show failed only" while tracking a regression across multiple eval iterations stays in that mode after a page refresh. The auto-clear path (failed count drops to 0) clears the localStorage entry too, so an accidentally-armed filter never ghosts across sessions.
- **`subscribeJob` reconnect-with-cursor.** `dashboard/frontend/src/v2/api/jobs.ts::subscribeJob` now accepts an optional `{ since }` option and forwards it as `?since=N` on the `/ws/jobs/{id}` URL; the backend's existing `since` support means a reconnecting client can skip events with `event_id <= since`. `JobLine` gained an optional `event_id` field carrying the server-assigned monotonic id, so callers can track the high-water mark and pass it back on reconnect to avoid re-receiving already-displayed lines. Additive: no behavior change for current callers.
- **KNOWN_ISSUES.md #2 marked OBSOLETE.** The "WS reconnect close handler shares mutable conn ref" issue described `store.ts:329, 389` and the `unsubscribeJob` helper, both deleted in the v2 dashboard rewrite. The current shared WS in `v2/api/v2ws.ts` already implements the recommended `_ws === ws` close-handler guard. Cleaned up the entry so future maintainers do not chase a non-existent bug.

### Fixed

- **Backend job-persistence sqlite mirror was growing unboundedly.** `JobPersistence.delete_old()` was defined but never called by any caller, so every spawned job permanently added a row to `.evalyn/data/jobs.sqlite`. Over months of use the file would grow to MBs and `/api/jobs/recent` queries would slow proportionally. `JobManager` now invokes `_persist_gc_maybe()` at the end of every `_persist_job_terminal`; it counts terminal events and prunes the table to the most-recent `persistence_keep` rows (default 200) every `persistence_gc_interval` terminals (default 50). Both are tunable via constructor args. GC runs in its own try/except so a prune failure cannot poison the per-job persist path. Adds three regression tests: GC fires on terminal at threshold, GC is skipped below the interval, and GC is a safe no-op when persistence is disabled.
- **Subscriber race in `AgentRuntime.subscribe()`** (KNOWN_ISSUES.md #3). Same pattern as the JobManager fix below: `_AgentEventStream` gained a replay phase with `_live_buffer`, `_replay_put`, `_begin_replay`, `_end_replay`. `AgentRuntime.subscribe` now registers the stream BEFORE replay, snapshots `_next_event_id`, replays via `_replay_put` (bypasses the buffer), and flushes concurrent `_emit` events at the end. Confirmation_required events emitted during the previous registration gap are no longer dropped; the chat UI cannot get stuck waiting for them.
- **Subscriber race in `JobManager.subscribe()`** (KNOWN_ISSUES.md #1). Live job events emitted between the replay phase and subscriber registration could be silently dropped on slow or freshly-attached WebSocket clients. `_EventStream` now buffers concurrent `_emit()` events into `_live_buffer` during replay; `subscribe()` registers the stream first, snapshots `_next_event_id`, replays from the snapshot, then flushes the buffer in order. Result: no event loss and no out-of-order delivery, even if future changes make queue puts yield mid-replay.

### Added

- **Co-pilot guidance tours.** Each tabbed route (Home, Datasets, Experiments, Review, Metrics) ships a short Driver.js-powered walk-through that auto-fires on first visit, anchored to stable `data-coachmark` attributes on real UI elements. Each tour has an independent `localStorage` completion flag, so finishing one does not suppress the others.
- **Tours menu in the AppShell header.** Always-visible button with a status dot (ember = on, muted = off). Click to: toggle all guidance off/on, manually start the current page's tour, or reset every per-tour completion flag in one action. Closes on outside click and Escape.
- **Per-route Settings reset.** The "Reset first-visit flags" action in Settings now clears every tour's completion flag (previously only the Home first-run tour). Sourced from `KNOWN_TOUR_IDS`, derived automatically from the tour registry.
- **Anchor-not-found policy.** If a tour step targets an element that is not in the DOM (empty workspace, conditional render), the engine waits 500ms then silently elides that step rather than deadlocking. If every step elides, the tour abandons without firing instead of showing an empty popover. The user sees a tighter sequence of real steps; manual re-trigger via the Tours menu still works once the page populates.
- **`prefers-reduced-motion` respected** for the focus ring; static outline replaces the pulse when the OS-level setting is on.

### Fixed

- **`dashboard/frontend/src/v2/tour/scripts/` was being silently gitignored** by the unanchored top-level `scripts/` rule. Added a `!` exception so all tour scripts are first-class source going forward; the Home `firstRun.ts` tour script that previously lived only on disk is now tracked in git.

## [0.1.0] - 2026-05-01

Initial release. Localhost IDE for evalyn evaluations, distributed as a separate optional package.

### Added

- **CLI catalog** - all 35 evalyn CLIs introspected from their argparse parsers and exposed as auto-generated forms. Groups: Tracing, Dataset, Metrics, Eval, Analysis, Annotation, Insights, Export, Simulation, Infrastructure, Quickstart.
- **Auto-form generation** - 7 param kinds (`bool`, `string`, `number`, `select`, `multiselect`, `path`, `long-text`) classified from argparse `Action` shape and dest-name heuristics. Three form modes: Form, Preview, Raw.
- **Subprocess streaming** - `JobManager` spawns each CLI as `["evalyn", <cmd>, ...]` via `asyncio.create_subprocess_exec`. Per-line stdout/stderr capture, fanout subscribe for multiple WebSocket viewers, backpressure with truncation marker, SIGTERM + 3s grace + SIGKILL on cancel, 60min default timeout, last 100 jobs retained in memory.
- **Terminal panel** - inline ~1KB ANSI parser, auto-scroll, one terminal view per job tab.
- **Jobs panel** - live state of running and recent jobs, click-to-open, per-row cancel.
- **AI chat agent** - dock-right `ChatPanel` with full agentic loop: text streaming, tool-call cards, confirmation cards (approve/reject), final-suggestion cards (clickable into pre-filled CLI form). Per-turn budget of 8 tool calls.
- **Multi-provider support** - `OpenAIProvider`, `AnthropicProvider`, `OllamaProvider` behind a shared `BaseProvider` interface. Provider-native tool-call streaming.
- **Credentials store** - `~/.evalyn/credentials.json` with atomic write and `chmod 600`. API never returns plaintext keys to the frontend. Per-provider `test` endpoint makes a 1-token completion call.
- **Settings UI** - `SettingsModal` with API-key input (password type), test button, model dropdown, active-provider radio.
- **Read-only allowlist** - 19 commands auto-run by the agent without confirmation. All other commands require explicit user approval via a confirmation card; 5min timeout defaults to rejected.
- **WebSocket transport** - `/ws/jobs/{id}` for job streams, `/ws/agent/{thread_id}` for agent events. Reconnect with `last_event_id`.
- **Localhost binding** - bound to `127.0.0.1` by default. `--unsafe-bind` required to override, with stderr warning.
- **CSRF protection** - per-server random token injected into served `index.html` as `<meta name="workbench-token">`. Required on all mutating routes via `X-Workbench-Token` header.
- **Plugin discovery** - registers the `dashboard` subcommand on core evalyn via the `evalyn.commands` entry-point group. No core changes needed at install time.
- **Pre-built frontend** - React 18 + TypeScript + Vite bundle vendored at `evalyn_dashboard/static/`. End users never run `npm`.

### Notes

- **Deprecation alias.** The previous `evalyn dashboard` command (a static HTML insights report) is renamed to `evalyn report`. When `evalyn-dashboard` is **not** installed, `evalyn dashboard` prints a stderr deprecation warning and forwards to `evalyn report`. The alias is removed in core evalyn v3.0.
- **Known issues.** See [KNOWN_ISSUES.md](./KNOWN_ISSUES.md). Two `IMPORTANT` items are tracked: (1) subscriber race in `JobManager.subscribe()` between replay and registration; (2) WS reconnect close handler shares mutable connection ref. Both have fix sketches; neither blocks v0.1.
- **State persistence.** Tabs, jobs, and chat threads are in-memory only. Server restart clears them. `.evalyn/` artifacts on disk are unaffected.
- **No telemetry, no auto-update, no daemon mode.**

### Implementation phases

For maintainers: v0.1.0 was built in 5 phases.

- Phase 0 - repo restructure: uv workspace, `dashboard/` package, Vite scaffold, shared `catalog.schema.json`, plugin entry-point on core CLI, rename `dashboard` -> `report` + deprecation alias.
- Phase 1 - foundation (5 parallel lanes): FastAPI skeleton + CSRF + localhost guard, argparse introspector, `JobManager` (spawn / cancel / fanout / backpressure / history), `CredentialStore`, frontend shell (`App` / `TitleBar` / `EditorTabs` / `BottomPanel` / Zustand store).
- Phase 2 - CLI execution (3 parallel lanes): `/api/cli` + `/api/cli/run` + `/api/jobs/*` + `/ws/jobs/{id}`, sidebar + `CliCatalog` + `CliForm` + `ParamField`, `Terminal` + `JobsList` + WS subscriber.
- Phase 3 - agent runtime (2 parallel lanes): `AgentRuntime` + 3 providers + tool loop + allowlist + confirmation gate + `/api/agent/*` + `/ws/agent/{id}` + `/api/settings/*`, `ChatPanel` + `SettingsModal` + tool/confirmation/suggestion cards.
- Phase 4 - polish (2 parallel lanes): Playwright E2E + CI matrix, docs + CHANGELOG + release.
